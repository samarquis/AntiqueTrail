# Auth layout visual evidence

Before and after captures for `/auth/sign-in` at 1280, 768, 390, 360, and 320 CSS pixels in light and dark color schemes.

Captured from the local review build with the existing review harness visible. Full-page PNGs preserve the complete auth flow surface; the 320px captures verify the signed-out five-item navigation stays in document flow instead of covering form controls.
