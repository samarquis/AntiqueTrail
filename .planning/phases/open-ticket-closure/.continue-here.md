---
phase: open-ticket-closure
task: 4
total_tasks: 6
status: in_progress
last_updated: 2026-08-04T19:45:55.324Z
---

<current_state>
All known code-level findings for open package tickets #15, #16, #17, #19, #20, and #29 are implemented through commit `05d8077`. The repository was clean before this handoff. The last whole-tree `npm run check` passed before the final auth/export, trip, and catalog/RG regression commits; each final commit passed its focused typecheck, lint, tests, SQL parsing, and diff checks. The next session must run one final whole-tree check before pushing.
</current_state>

<completed_work>

- Implemented shopper-private actions, candidate flows, trip Go/offline/summary/clone behavior, Browse map seam, partner claims/admin verification, RG authority, account lifecycle export/deletion, and fail-closed hosting/quota controls.
- Ran the Matt Pocock two-axis review twice and fixed all reported code findings, including authoritative 10-minute password+MFA proof, immutable store ID routing, claimant projection minimization, quota forecast/finality, bounded exports, final-stop completion, skipped/closed Undo, synchronized 500-point map filtering, and exact RG live-source scope.
- Local full check reached 51 test files / 280 application tests and 24 release tests before the final regression commits.
- Final focused validation: 46 trip tests, 18 auth/export tests, 31 catalog/App tests, typecheck/lint, pglast, and diff checks passed.
- All work through `05d8077` is committed on `main`; no server or watcher is running.
</completed_work>

<remaining_work>

- Task 4: Run `npm run check`, parse every SQL migration/test with pglast, verify migration versions are unique, and confirm a clean worktree.
- Task 5: Push `main` to `samarquis/AntiqueTrail`, watch GitHub CI/Supabase reset/pgTAP through completion, and fix any real CI failure.
- Task 6: Comment on #15, #16, #17, #19, #20, and #29 with code/validation evidence and change them from `ready-for-agent` to `ready-for-human`. Keep them open until hosted/provider/human evidence exists.
- Preserve the disabled candidate-cleanup workflow unless a deployed worker, secrets, and operational owner exist.
</remaining_work>

<decisions_made>

- External provider and human gates remain fail closed; never fabricate signatures, quota observations, delivery receipts, restore evidence, owner contact, or release evidence.
- H-01 scheduled monitoring performs a configuration-safe preflight so missing configuration causes no provider call and no recurring failure-email noise.
- Public catalog slugs are presentation identifiers only; private RPCs use server-resolved immutable store IDs.
- Privileged actions require provider-authoritative password and enrolled-MFA evidence within 10 minutes; session registration does not refresh authentication freshness.
- Portable export bounds are 100 media files, 8 MiB per source file, and 32 MiB total uncompressed source data until a streaming writer is implemented.
</decisions_made>

<blockers>

- Docker/Podman is unavailable locally, so real Supabase reset and pgTAP execution must occur in GitHub CI.
- Supabase/Cloudflare are not linked/configured for deployment in this workspace; production credentials and exact environments remain human actions.
- H-01, E-01, R-01, M-01, L-01, S-01, HC-01/02, SEC-01, B-01, SLM/beta/release/promotion/RG/activation require real provider or named-human evidence.
</blockers>

<context>
The safest stopping point is commit `05d8077`. The remaining work is integration validation and external evidence coordination, not another broad feature pass. Start by trusting the focused validation but verify the combined tree once. If CI is green, do not close acceptance-gated issues merely because code is complete; attach commit/test evidence and hand them to humans/providers.
</context>

<next_action>
Start with: `npm run check`, then pglast-parse all files under `supabase/migrations` and `supabase/tests`, verify unique 14-digit migration versions, push `main`, and watch the resulting GitHub Actions run.
</next_action>
