# Gates: Triage issue #134
Scope: read-only classification of issue #134.
- [x] G1: Contract and source paths inspected.
  EVIDENCE: PRD.md:799-803 restricts each draft to its bound identity and Administrators. src/features/partners/components.tsx:273-372 renders all editable draft inputs synchronously and calls only saveDraft/submitDraft; it never loads status before render. App.tsx:1137 mounts this route without a UI guard. The authoritative RPC path already derives actor identity and rejects an unbound identity (supabase/migrations/20260818400000_candidate_partner_commands.sql:90-103); review harness client similarly rejects non-Representative callers (src/review-harness/clients.ts:1852-1955). Existing component and e2e tests cover successful editing, not denied access before fields render (components.test.tsx:221-232; e2e/ui08-partner-portal.spec.ts:70-85).
- [x] G2: Ready, minimal, or blocked classification recorded.
  EVIDENCE: MINIMAL_IMPLEMENTATION. Add an initial authoritative client.getStatus() gate to PartnerDraftPage, render only loading or GenericPartnerError until it resolves, and allow the form only for the bound identity in an editable onboarding state. Add unit coverage that no editable controls render while status loads or rejects, plus denied-identity browser coverage for /partner/draft. No new backend authorization surface appears necessary: partner_safe_command already session-binds the identity and fails closed.
