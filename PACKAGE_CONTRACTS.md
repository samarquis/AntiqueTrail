# Regional Public MVP Package Execution Contracts

Status: normative engineering handoff for Packages 2â€“13. This file translates approved behavior into bounded implementation contracts; it does not report current completion and does not override product behavior in `PRODUCT_DECISIONS.md`, interaction behavior in `DESIGN.md`, visual rules in `DESIGN_SYSTEM.md`, or security policy in `SECURITY_AND_TRUST.md`. Package 1 remains controlled by the bounded contract in `IMPLEMENTATION_PLAN.md`; current implementation and release state live in `PROJECT_STATE.md`.

## Shared execution rules

Every package uses the single React/TypeScript/Vite PWA and Supabase/PostgreSQL boundary in ADR 0004. Create only the active package's routes, tables, functions, jobs, tests, and evidence. No placeholder scaffolding for later packages.

- **Identifiers and records:** UUID primary keys generated server-side; UTC `timestamptz`; immutable `created_at`; server-maintained `updated_at`; mutable aggregate/state roots carry positive `version bigint`. Human-facing slugs are never authorization keys.
- **Authorization:** base tables have RLS and no `anon` direct grants unless Package 1 explicitly allows an RPC. Clients cannot choose owner, role, scope, stage, aggregate, verification, audit, or retention fields. Server commands recheck authenticated identity, active grants, resource scope, stage, and current version.
- **Commands:** mutations run through fixed-search-path database RPCs or Edge Functions named below. Each accepts an idempotency key where retry is possible. Success returns resource ID, new version/state, server time, and allowed next actions. Errors use `validation_failed`, `authentication_required`, `recent_auth_required`, `mfa_required`, `not_found`, `not_allowed`, `conflict`, `rate_limited`, `stage_disabled`, `provider_unavailable`, or `internal_error`; hidden-resource and account-matching paths conflate `not_found`/`not_allowed` as specified by security policy.
- **Concurrency:** commands lock the state root, compare expected version, apply business rows plus required audit/outbox rows in one transaction, and return the prior success for a repeated idempotency key. Stale writes return `conflict` with current version and only fields the caller may already read. Silent last-write-wins is forbidden.
- **Jobs:** scheduled workers take an advisory singleton lock, claim bounded batches with `FOR UPDATE SKIP LOCKED`, record attempt/result, and are idempotent by `(job_type, resource_id, due_at)`. Failure retries with capped exponential backoff; exhausted work opens an operations case without logging private payloads.
- **Time:** database `statement_timestamp()` is authoritative. User/store calendar rules use the stored IANA time zone. Test clocks exist only in local/test migrations and are inaccessible in production.
- **Data lifecycle:** every table follows `SECURITY_AND_TRUST.md`; deletion jobs remove dependent Storage objects, write content-free receipts, and are replayed after restore before access resumes. Exports omit other-user, secret, moderation, verification, and purged precise-location data.
- **Migrations/rollback:** additive schema and disabled server capability first; backfill with counts/checksums; enable only after tests. Rollback disables capability/routes before reverting compatible code. Destructive rollback uses the prior database/Storage restore point and deletion-receipt replay. Never roll back by weakening RLS or restoring revoked grants.
- **Quality budget:** zero known authorization/privacy/data-loss defects; WCAG 2.2 AA; approved browser/device matrix; no unhandled console error. Non-provider commands target p95 â‰¤1 second and p99 â‰¤2 seconds under the package load profile. User-visible provider/upload jobs show progress by 1 second, never block the main thread, and use the package timeout/fallback. Each package records query plans for high-volume paths and sets rate/cost limits before enablement.
- **Evidence:** attach migrations, schema diff, allow/deny matrix, state-transition tests, browser/accessibility proof, performance/security results, job/retry/restore proof, rollback result, and dated owner disposition to GitHub Issue #1. A failed required check blocks the package.
- **Database privileges:** enable and `FORCE RLS` on application tables. Runtime/authenticator roles never own tables. Revoke schema/table/sequence/function privileges from `PUBLIC`. Prefer `SECURITY INVOKER`; every necessary `SECURITY DEFINER` function is owned by a dedicated non-login non-table-owner role without `BYPASSRLS`, uses a literal fixed search path and schema-qualified objects, validates session/stage/role/scope, and has direct-table plus sibling-row denial tests. Service-role operations are isolated approved jobs/functions only.
- **Sessions:** Package 2's application session registry and `current_session_is_active()` gate every private/privileged table, Storage policy, RPC, Edge Function, export, signed URL, and offline sync. Provider revocation failure never restores application access. Refresh material uses only the dedicated IndexedDB adapter; access tokens stay in memory.
- **Cache and headers:** shell/service worker `no-cache`; hashed static assets one-year immutable; all startup API/RPC `no-store`; authenticated/mixed/pilot/readiness/operational responses `private, no-store` plus `Vary: Authorization, Origin`. Service worker caches no API/private data. Every route/error/redirect passes the exact CSP/security-header contract in `SECURITY_AND_TRUST.md`.
- **Validation:** every field enforces the global code-point/byte/normalization/control-character/rendering matrix in `SECURITY_AND_TRUST.md` plus narrower package limits. Prose is text-only; URL fields alone become validated links. Preview/publication share one escape path.
- **Capabilities:** server stages/capabilities default false and are checked by RLS, Storage, RPC, Edge, route loader, job, and signed-URL issuer. Only a receipt-bound deployment command changes them atomically. Client flags never authorize. Rollback disables capabilities first.
- **HMAC/audit:** use independent purpose/environment HMAC keys with version/rotation lifecycle. Privileged audit is append-only hash-chained and externally anchors content-free roots; missed root for 24 hours disables privileged mutations. No package may claim immutable audit without this proof.
- **Provider/cost gates:** H-01 precedes any shared environment; E-01 real email; R-01 Package 5B; M-01 any real media; L-01 external audit-chain anchoring before privileged shared/external use; S-01 first owner contact; SEC-01 independent security review and B-01 final brand/domain before Package 10B; A-01 optional analytics. ADR 0006's Vercel deployment boundary and ADR 0005's retained `$0`, 25% headroom, 75% pause, 90% degradation, no-overage, full DB/Auth/Storage restore, and public-release block are normative. A failed gate blocks only its dependent capability.
- **Human capacity:** HC-01 before first owner contact names Product, Engineering/Security/Operations, support/on-call primary and human backup, second catalog verifier, legal/insurance contacts, and any independent reviewer. HC-02 before public promotion names routine moderation, independent appeal, support, on-call backup, two catalog verifiers, accessibility/usability, and incident communication owners and rehearses handoffs. An AI is never human coverage or independent approval.
- **Feature restrictions:** automated signals and rate limits may open a case but never impose a restriction. Only a final case-scoped MFA/recent-auth decision may restrict the same abused feature/store scope. First upheld case in 180 days gives notice only; second in 180 days permits 30 days; third in 365 days permits 90 days; fourth in 365 days permits 180 days only after a different qualified reviewer signs the evidence. Counts never cross feature types. Commands are `open_abuse_case`, `finalize_abuse_case`, `impose_feature_restriction`, `submit_restriction_appeal`, `decide_restriction_appeal`, and `expire_feature_restriction`. One appeal may be filed within 30 days and is decided by a different qualified reviewer. Expiry is automatic. Tests cover thresholds, notice, exact scope, denial without hiding existing content-management/export/delete/appeal, different-reviewer decision, expiry, and no shadow/global suspension.
- **Clean reproduction:** package close requires a clean checkout to reproduce migrations/fixtures, all allow/deny and lifecycle tests, rollback, security/privacy artifacts, and dated receipt. Every PR runs format/lint/types/focused tests/secret scan/Chromium smoke; package/stage closes run the full applicable matrix. Free CI exhaustion delays work; it never skips required authorization/privacy/migration/release checks.

## Package 2 â€” Identity, sessions, roles, audit, and account lifecycle

`completed_terminal_cleanup` is a derived privacy predicate, never an admission state: `state=cleanup_pending`, authoritative provider deletion/absence and every non-metadata cleanup step are terminal, and only deletion-only admission-metadata clear plus final linkage purge remain. It cannot authorize registration, delivery, verification, profile creation, or any state transition except completing that privacy purge.

**Admission contract:** direct anonymous provider signup stays disabled in every environment. `account_registration_mode=closed|receipt_only|public` is server-owned and defaults `closed`; restricted one-use admission records follow `issued â†’ claimed â†’ provider_pending â†’ provider_created â†’ delivery_pending â†’ verification_pending â†’ active`, with terminal `expired|revoked|orphan_quarantined` and recoverable `cleanup_pending`. Receipt-only admission is delivered as an independently generated 256-bit secret in `/auth/register#receipt=<opaque>`; the shared fragment rule scrubs it before render/network, keeps it only in memory, and supplies it to `begin_account_registration(receipt_token, email, age_18_attestation, password, idempotency_key)`. The Edge Function constant-time verifies the purpose/environment-keyed token hash and exact approved, unused, verified-email-bound receipt before `claim_expires_at`; claim expiry governs only whether registration may begin. Missing, malformed, wrong-email, expired, revoked, or replayed secrets return the same generic unavailable state. Once timely claimed, completion eligibility uses the separately persisted provider verification deadline and authoritative provider confirmation time, never the earlier claim expiry. In `public`, `receipt_token` must be absent and the same function first applies registration rate/age checks and internally issues the admission recordâ€”no caller may mint one. Before any provider call it persists a server-generated admission UUID, normalized email HMAC, age-18 attestation time, and `provider_pending` intent.

The server passes password only in memory to `auth.admin.generateLink({type:'signup', email, password, data:{antique_trail_admission_id:<UUID>}, redirectTo:<allowlisted app origin>})`. It discards and never sends the returned provider `action_link`. On an exact response it stores the provider user ID, copies the unguessable reconciliation UUID into service-controlled `app_metadata.antique_trail_admission_id` with `auth.admin.updateUserById`, clears the mutable metadata copy, and constructs only `/auth/callback#token_hash=<returned properties.hashed_token>&type=verify`. Before E-01 send it persists a unique delivery idempotency key and `delivery_pending`; the custom mailer disables link rewriting/tracking/prefetch and sends that app-origin fragment URL. No provider `/verify?token=`, action link, OTP, password, access token, or refresh token is persisted, logged, or emailed. A confirmed E-01 acceptance sets `verification_pending`; an E-01 timeout queries the same delivery key and never guesses delivery state. Provider-unknown status after the E-01 bounded reconciliation window enters `cleanup_pending` under CAS.

H-01 must prove two private fixed-search-path Auth lookup helpers with the session helper's ownership/grant restrictions. The admission helper accepts only the unguessable server-generated admission UUID, uses one static exact metadata query, and returns provider ID, normalized email, confirmation time, and required reconciliation metadata; the application purpose-keyed-HMAC-compares the returned normalized email to the admission receipt. The provider-ID helper accepts one recorded provider user ID and returns only authoritative existence, confirmation time, and required cleanup metadata. Neither helper scans or accepts a partial identifier. Zero, multiple, HMAC/metadata mismatch, or helper failure denies. An ambiguous `generateLink` response cannot supply a trusted hash, so an exact admission match enters `cleanup_pending`; a mismatch takes the generic existing-account path. Metadata failure or confirmed non-delivery may delete the exact unverified provider user immediately. Possibly delivered/unknown mail never does: persist `verification_link_expires_at`, wait through provider expiry plus five minutes of clock skew, then CAS the receipt and reread exact confirmation before any provider call. Only a nonrevoked exact user whose authoritative `email_confirmed_at <= verification_link_expires_at + 5 minutes` moves to `verification_pending`; claim expiry is ignored after a timely claim. Missing or later confirmation remains cleanup-bound and receives idempotent session revocation/deletion. Never hold a database/Auth row lock across that HTTP call. After delete success or timeout, reread exact provider state: absent completes cleanup; newly observed eligible confirmation reconciles; revoked or late confirmation stays deletion-bound; still-unconfirmed retries. Application state denies the user throughout any ambiguous delete. Delivery timeout and cleanup serialize their own receipt transitions with expected versions, not the external provider operation.

Every external registration operation first opens a durable intent under the single lock order `quarantine latch â†’ provider operation â†’ quarantine subject â†’ admission/config`. Forward kinds `generate_link|set_metadata|send_verification` may reserve only while the latch is `open` for an eligible active admission/config. Cleanup/status kinds `exact_lookup|session_revoke|exact_user_delete|delivery_reconcile|escalate` may reserve while `open` for an existing `cleanup_pending` admission/operation; the one additional open-state pair is exact `delivery_pending` admission + its `send_verification` operation in `reconciliation_required`, which may reserve read-only `delivery_reconcile` by that delivery idempotency key. They may reserve while `draining|blocked` only for a preexisting operation/subject and cleanup-bound admission. The separate privacy-cleanup kind `clear_admission_metadata` may reserve under `open|draining|blocked` only for an exact provider ID + admission UUID whose admission is `active|expired|revoked|cleanup_pending|orphan_quarantined|completed_terminal_cleanup` or whose account deletion is in progress. It may only remove `app_metadata.antique_trail_admission_id` after equality with that UUID; absent is idempotent success, while mismatch/unknown denies. Cleanup/status/privacy operations can never create, add/change any metadata value, send, redeem a token, alter admission/account/profile state, or activate a profile. The reservation stores kind/class and expected latch, admission, and config versions. Before any external call, the caller must receive acknowledgement for an encrypted write-ahead entry in the registration safety journal defined below; journal failure cancels the reservation and no provider call occurs. The `reserved â†’ calling` transaction takes every lock in order and revalidates journal acknowledgement plus the class-specific predicate: a forward kind requires `open`, unchanged expected versions, active receipt/config eligibility, and no expiry/revocation; cleanup/status requires its exact preexisting operation/subject/provider/admission scope and allowed state, with the delivery-pending pair exact and `draining|blocked` valid only for cleanup-bound scope; privacy cleanup requires the exact bound provider/UUID and allowed terminal/privacy state under any latch state. Any failure sets `cancelled_before_call` and no provider call occurs. Any local mutation that revokes, expires, replaces, or otherwise removes admission eligibility uses the same lock order, cancels all still-reserved forward operations, and marks any `calling|reconciliation_required` forward operation plus its admission cleanup-bound before committing. It never waits on or assumes cancellation of an external call. No database/Auth lock crosses the external call. On return, the caller locks in order and repeats the same class-specific version/scope predicate before recording `settled_no_effect|settled_captured`. A forward result with non-open/stale/revoked/expired/replaced/cleanup-bound state suppresses every next delivery/application-activation step, resolves the admission UUID, records any provider ID as a quarantine subject, and keeps the receipt cleanup-bound/orphan-quarantined. A cleanup/status/privacy result may continue only its exact lookup/revoke/delete/metadata-clear/reconcile/escalate outcome. For the exact timed-out delivery pair, one transaction locks the original send operation, status operation, and admission. Authoritative `delivered` with open latch/current versions/eligibility sets original send `settled_captured`, status operation `settled_no_effect`, and admission `delivery_pending â†’ verification_pending`. Authoritative `delivered` with any non-open/stale/revoked/expired/replaced/cleanup-bound state instead sets original send `settled_captured`, status operation `settled_no_effect`, admission `cleanup_pending|orphan_quarantined` as applicable, captures the exact provider user as a quarantine subject, and permits no delivery/application-activation continuation. Authoritative final `not delivered` sets both operations `settled_no_effect` and admission `cleanup_pending`. `Unknown` leaves the original send `reconciliation_required` and admission `delivery_pending` (or cleanup-bound after its bounded window) while settling only the completed status operation `settled_no_effect`. Any other mismatch stays nonterminal and denies rather than becoming a forward action. Response loss or timeout moves the intent to `reconciliation_required`, never terminal by elapsed lease alone; every timeout/finality reconciler takes the same locks and repeats the class-specific checks before it may settle absent or capture an effect. H-01 must establish a tested provider-call deadline and authoritative finality window for each operation kind; only after that window and exact UUID/provider-ID/delivery-idempotency reconciliation may an absent effect settle. If finality cannot be proved, the intent remains nonterminal, registration remains closed, and Operations/Security escalates rather than guessing. Tests interleave each forward, cleanup/status, and privacy class, including metadata clear for every allowed admission/account-deletion state under every latch state, mismatch/unknown/idempotency/no-other-metadata mutation, delivered-current, delivered-stale/non-open, final-not-delivered, and unknown atomic pair settlement, through reservation/call/return/timeout with revoke, expiry, reissue, config change, wrong subject/provider/key, and drain/clear.

Reconciliation polls at 1, 5, and 15 minutes; provider deletion starts only at the safe boundary above, then retries at 5, 15, and 60 minutes and every six hours. One hour after the safe delete boundary, failure pages Operations/Security. At 24 hours after that boundary, the orphan transaction takes the required locks in order, changes `open â†’ draining`, closes registration mode, records the known provider ID/deletion ticket, and marks the receipt `orphan_quarantined`. `draining|blocked` denies every new admission, receipt issue/exchange, create/update/send operation, application verification completion/profile activation, registration-mode opening, and Package 10B promotion for every email. A previously delivered provider token may still be redeemed directly at Supabase: the callback may confirm provider identity but must then see the non-open latch, deny application completion, record/capture the confirmed provider user, revoke its session, and delete it at the safe cleanup boundary. It explicitly permits only exact subject-scoped lookup/revocation/deletion/reconciliation/escalation, deletion-only `clear_admission_metadata`, and signed latch-clear proof needed to remove the orphan; privacy metadata clear follows its exact bound UUID/provider predicate under every latch state and can never add/change metadata or admission/account state. Cleanup can never create, deliver, or activate an Antique Trail registration. The drainer cancels every still-reserved `generate_link|set_metadata|send_verification` intent and captures every preexisting calling/reconciliation intent as above. Only when all such intents are terminal and every affected receipt/created user is captured does one transaction change `draining â†’ blocked`; an unresolved intent keeps the latch `draining` and mode `closed` indefinitely. Provider deletion continues daily; 72-hour failure fails H-01 and requires provider escalation/incident handling, but never broadens the cleanup allowlist.

Before the first shared-stage provider call, H-01 registers two distinct named human deployment-recovery signersâ€”one Product signer and one Security signerâ€”outside Antique Trail accounts and the restored application database. Each separately controls an offline Ed25519 private key; H-01 binds the public-key fingerprints and signer roles in protected deployment configuration and proves canonical-byte signing/verification, separate custody, loss/rotation/revocation, wrong-key denial, and no private key in CI, repository, database, logs, or backups. This authority grants no application role or data access and is independent of Package 8 evidence responsibilities. If either required signer/key is unavailable, provider registration stays disabled; no one-person fallback exists.

Deployment-only `clear_registration_quarantine(signed_reopen_receipt, expected_latch_version)` requires `blocked`, locks latch then operations then subjects, denies while any operation is nonterminal, and uses the hosted exact provider-ID helper to prove every recorded subject authoritatively absent. The one-use receipt contains a server/deployment-issued 256-bit nonce and expires after 30 minutes; the distinct registered Product and Security deployment-recovery signers sign the same canonical bytes, binding environment, nonce, expected latch version, exact subject-set digest, provider-absence observation times/helper version, external journal sequence/root, reason, and intended `blocked â†’ open` transition. Replay, signer/key collision, missing/revoked/wrong signature, expiry, stale version/digest, present/unknown ID, new cleanup reservation, or helper/journal failure denies and audits no sensitive payload. Success atomically burns the nonce, writes a content-free outcome receipt, changes only `blocked â†’ open`, and leaves registration mode `closed`; a separate new signed stage/release receipt may then change mode. Within 24 hours after verified absence and clear, delete the subject row, provider ID, deletion ticket, admission UUID/linkage, provider/delivery idempotency data, and terminal operation rows from both database and external encrypted journal versions; backups age those identifiers out within 30 days. Retain only outcome, timestamps, journal sequence/root, and signer roles/key fingerprints for 90 days, outside shopper export, ordinary logs, telemetry, and support. A quarantined or expired receipt never reopens automatically. Tests cover latch CAS/restore; mandated lock order; reservation versus drain; return before/after `draining|blocked`; provider token redemption in both states; unknown, timed-out, and late provider effects; same/different-email denial; cleanup allowlist and creation bypass denial; nonterminal-clear denial; subject added by a late return; present/unknown/absent multi-subject clear; first-Administrator and first-secondary-account quarantine; signer/key custody/rotation/revocation/expiry/replay/digest denial; privacy purge; Package 10B denial; and signed reopen with mode still closed.

**Registration restore fence:** before any shared-stage provider call, H-01 must prove a `$0`, integrity-protected registration safety journal outside the Supabase application database and every database-backup rollback domain. Its encrypted write-ahead entries contain only monotonic sequence, prior root, operation/admission UUID, operation kind/state/deadlines, delivery idempotency key, provider ID when learned, quarantine/clear transition, and ciphertext digestâ€”never email, password, token, link, or content. A separately retained content-free sequence/root detects rollback; identifier-bearing journal versions purge on the same 24-hour/30-day lifecycle above. Every restore occurs on an unroutable target under a deployment-level recovery fence that denies registration independently of restored database state. Before any traffic, overwrite restored mode to `closed` and latch to `draining`, compare/replay the external journal high-water plus deletion/revocation receipts, reconcile every operation/admission/provider ID, and invalidate pre-restore sessions. Missing journal data, root/sequence mismatch, unknown provider effect, or nonterminal operation keeps the fence on and stage closed. Fence removal uses the same registered Product/Security deployment-recovery signers and canonical two-signature verifier as quarantine clearâ€”not application MFA/recent-auth, because no valid restored account is trusted yet. Its separate one-use 256-bit nonce expires in 30 minutes and binds environment, recovery-fence deployment digest/version, restored backup-set digest, reconciled journal sequence/root, exact operation/subject-set digest, target latch version, reason, and only `draining â†’ blocked`; missing/revoked/wrong/colliding signatures, expiry, replay, stale digest/version, or unknown state denies. Success burns the nonce and writes a content-free signed audit receipt outside the restored rollback domain. Normal signed clear may then open the latch, while registration mode remains closed. Tests restore at reservation, calling, response-loss, capture, drain, blocked, clear-before-purge, and post-purge boundaries, plus every signer/nonce/digest/version/replay denial. If the journal or two-signer fence proof cannot be proven at `$0`, shared startup registration remains disabled and testing stays local.

**Registration-mode, receipt issuance, and raw-secret handoff:** deployment-only `set_account_registration_mode(mode, signed_stage_receipt, expected_version)` locks the quarantine latch before the singleton configuration and requires `latch.state=open` for every non-`closed` transition; both `draining` and `blocked` deny. First-Administrator bootstrap is a third explicit purpose of the pre-stage Product/Security deployment-recovery signer system. A separate server/deployment-issued 256-bit one-use nonce expires after 30 minutes; both registered signers sign the same canonical bytes binding environment, nonce, exact initial normalized-email HMAC, `purpose=initial_admin`, expected latch/config versions, `closed â†’ receipt_only`, and 30-minute claim expiry. Wrong/missing/revoked/colliding signatures, expiry, replay, email/purpose/version mismatch, or non-open latch denies. One locked transaction verifies both signatures, burns the nonce, changes mode, and creates exactly one exact-email initial-admin receipt plus raw registration URL. No application account/role authorizes this bootstrap. The protected no-log/no-artifact deployment response exposes that URL once only to the named initial Administrator; loss requires a new dual-signed revoke/reissue receipt and never reconstructs a raw secret. Tests cover canonical bytes, wrong signer/email/environment/purpose/version, nonce expiry/replay/atomic burn, partial failure, and quarantine during the first account. After bootstrap, a signed environment-stage receipt keeps shared Synthetic Alpha/readiness/Private Beta at `receipt_only`; rollback or missing/expired stage evidence returns `closed`. Only Package 10B `promote_release` may request `public`, and it requires the same locked-open latch through the whole release transaction. Direct `issue_stage_admission_receipt` locks the latch, requires `open`, then requires an active Administrator with MFA/recent-auth, exact email, `purpose=synthetic_tester`, linked test receipt, reason, and claim expiry no longer than 24 hours; its `private, no-store` response returns the full URL once to that Administrator for direct delivery to the named tester. No later read returns it; loss requires revoke/reissue.

Partner/readiness parent invitations never pre-create an unrecoverable child secret. Their fragment-token exchange locks the quarantine latch first and holds it through the local transaction; only `open` may verify the parent secret/exact email, burn the one-use parent, issue the child admission hash, and return the raw `/auth/register#receipt=...` URL directly to that browser over `private, no-store`; existing verified users receive normal sign-in instead. The original parent can never rotate, revoke, or redisplay a child. If the child URL is lost before claim, an Administrator/recruiter repeats exact-email/identity verification; a locked-open transaction revokes the unused child/pending record and issues a wholly new parent invitation. A stolen old parent remains useless. Parent revoke/expiry before exchange and explicit recovery after exchange cascade to every unused child. All responses, screens, clipboard/QR actions, proxy/access logs, telemetry, support, and CI artifacts follow the shared fragment-secret rule. Tests cover each authorized recipient/channel, exchange-versus-drain and recovery-versus-drain, atomic parent burn/child issue, exact once/read denial, lost-child verified revoke/new-parent flow, old-parent replay denial, bootstrap atomic failure, rollback closed, public denial outside 10B, wrong stage/email/purpose, missing signatures/MFA/recent-auth, duplicate/idempotent replay, expiry/revoke, parent mismatch, partial failure, and zero privilege before verified profile plus explicit grant.

**Admission privacy lifecycle:** raw admission and provider action-link secrets exist only in the intended recipient's fragment or server memory and never enter logs, telemetry, analytics, browser storage, exports, backups as newly written plaintext, or support/audit payloads. Within 24 hours after `active`, `expired`, `revoked`, or completed terminal cleanup, delete `token_hash`, email HMAC, idempotency key, provider-call timing, reconciliation metadata, provider linkage, and Auth `app_metadata.antique_trail_admission_id`; account deletion triggers the same purge immediately and deleted remnants age out of backups within 30 days. The privacy job first purges every field except the exact provider ID/admission UUID needed for deletion-only `clear_admission_metadata`; only authoritative field absence lets the same workflow delete that final linkage and terminal operation evidence. Failure at 24 hours opens a privacy/security incident and disables shared registration; it retains only that restricted pair solely for retry and never reports purge success. Retain only purpose, content-free outcome, and timestamps after successful clear: 90 days for initial-admin/synthetic/public registration security evidence, three years after relationship end for partner/readiness consent history, or two years when the event is already part of privileged audit. These records are excluded from shopper export and ordinary logs; an authenticated privacy request receives only the person's visible registration status, never tokens, HMACs, provider evidence, or internal identifiers.

**Outcome and scope:** verified-email 18+ shopper accounts; MFA/recent-auth for privileged roles; secure recovery/session revocation; exact store-scoped grants; append-only hash-chained audit with external root anchoring; account export/deletion/inactivity. No shopper feature data, partner onboarding, moderation, or external participants.

**Routes/states:** the authentication routes and states in `DESIGN_SYSTEM.md`, including `/auth/callback`; `/account/privacy`, `/account/export`, `/account/delete`, `/account/delete/cancel`, `/account/restrictions`; Administrator test-only role/session setup. Account states are `pending_verification`, `active`, `deletion_scheduled`, `deleted`; session states are `active`, `cancellation_only`, `revoked`. Export states are `queued`, `building`, `ready`, `failed`, `expired`. Grant states are `pending`, `active`, `revoked`, `expired`.

**Authentication contract:** Supabase Auth owns credentials. Register with normalized verified email, age-18 attestation, and password 12â€“128 characters; allow spaces/password managers, reject known-compromised values when the provider check is available, and impose no arbitrary composition rule. Verification/recovery email targets `/auth/callback#token_hash=<provider-token-hash>&type=<verify|recovery>` under `Referrer-Policy: no-referrer`; the callback is outside service-worker navigation caching, loads no third party, copies the fragment credential to memory, immediately calls `history.replaceState` to `/auth/verify` or `/auth/recovery`, then performs one POST exchange with Supabase. UI `verify` maps only to `verifyOtp({token_hash, type:'email'})` for provider signup; UI `recovery` maps only to provider type `recovery`; every other type denies. The hash never enters access/proxy/application/analytics/error logs or browser storage. Links are single-use and use the provider's shared 30-minute expiry. Replayed/expired/wrong-type callbacks show the same generic terminal state and generic resend/start-over action. Password recovery revokes all refresh sessions and sends a generic account-change notice. Access tokens expire after 15 minutes; rotating refresh sessions expire after 30 days of inactivity; refresh-token reuse revokes that session family. Sign-out revokes the current session; `Sign out all devices`, password recovery, and account deletion revoke all sessions. Administrator and Representative require TOTP MFA with ten one-time hashed recovery codes; privileged recent-auth means password plus MFA completed within 10 minutes. Shopper MFA is optional. Registration, resend, sign-in, and recovery return enumeration-resistant responses and per-account/IP/device rate limits. Tests inspect the exact emailed URL, history, referrer, Cache Storage, telemetry, and logs and prove no provider action-link query, access token, refresh token, or other bearer survives exchange.

**Schema and ownership:** singleton `account_registration_config(mode, stage_receipt_id, version)`; singleton `registration_quarantine_latch(id=1, state open|draining|blocked, draining_at, blocked_at, reopen_receipt_id nullable, external_journal_sequence, external_journal_root, version)`; restricted `registration_provider_operations(operation_id UUID PK, admission_id, kind, state reserved|calling|reconciliation_required|cancelled_before_call|settled_no_effect|settled_captured, expected_latch_version, expected_admission_version, expected_config_version, provider_user_id nullable, external_idempotency_key nullable, journal_sequence, journal_acknowledged_at, call_started_at, call_deadline, finality_due_at, settled_at, version)`; restricted `registration_quarantine_subjects(provider_user_id PK, deletion_ticket_id, quarantined_at, resolved_absent_at nullable)`; `profiles(user_id PK, verified_email_snapshot, public_display_name, age_18_attested_at, status, last_authenticated_at, session_epoch positive bigint, sessions_revoked_before, deletion_due_at, version)`; restricted `account_admission_receipts(admission_id UUID UNIQUE, token_hash, purpose, email_hmac, age_18_attested_at, idempotency_key, provider_user_id, provider_call_started_at, delivery_idempotency_key, delivery_state, claim_expires_at, verification_link_expires_at, cleanup_due_at, state, claimed_at, updated_at, version)`; restricted `app_private.active_sessions(session_id PK, user_id, provider_created_at, session_epoch, state, created_at, revoked_at, revocation_reason, version)`; `role_grants(subject_user_id, role, store_id nullable, state, granted_by, granted_at, revoked_by/reason/at, version)` with one exact store for Representative and no store for Administrator; `feature_restrictions`; `admin_bootstrap_state(nonce_hash, expires_at, completed_at, receipt_id, subject_user_id, subject_binding_expires_at, subject_binding_state active|cleanup_only|cleared)`; `session_security_events`; partitioned append-only hash-chained `privileged_audit_events`; `audit_chain_roots`; `denied_attempt_outbox`; `provider_revocation_outbox`; `account_export_jobs`; `account_deletion_requests`; `deletion_receipts`; `job_runs`; `notification_deliveries`. Inactivity uses authentication-owned `last_authenticated_at` only; browsing, trips, location, device activity, and catalog-last-seen cannot change it. Users read/update only allowed own profile/lifecycle/export/restriction status; no application role updates/deletes audit rows.

**Server commands:** `set_account_registration_mode`; `clear_registration_quarantine`; `issue_stage_admission_receipt`; `begin_account_registration`; `complete_account_registration`; `profile_update`; `register_current_session`; `session_list`; `session_revoke`; `session_revoke_all`; `require_recent_auth`; `bootstrap_initial_administrator`; `reset_expired_initial_administrator_bootstrap`; `grant_synthetic_test_role`; `revoke_synthetic_test_role`; `request_account_export`; `issue_export_download`; `request_account_deletion`; `cancel_account_deletion`. Every provider-facing registration command follows the latch-first reservation state machine above before receipt/config state; cleanup uses only its explicit allowlist. Every local admission mutationâ€”claim/begin, receipt issue or recovery reissue, parent exchange/child issue, registration resend intent, completion/profile activation, and bootstrap admissionâ€”locks the latch first, requires `open`, and holds that lock through its database transaction; ordinary sign-in and password recovery for an existing active account remain available. After callback token exchange, idempotent `complete_account_registration` derives the verified provider user/email, authoritative confirmation time, and current session from trusted Auth; locks the latch first and then the matching `delivery_pending|verification_pending|cleanup_pending` receipt/exact provider row; and requires an `open` latch, a timely prior claim, provider user ID/email HMAC/metadata match, nonrevoked/nonquarantined state, and `email_confirmed_at <= verification_link_expires_at + 5 minutes`. It does not reapply `claim_expires_at` after claim. An eligible confirmed `delivery_pending|cleanup_pending` row first reconciles to `verification_pending` under the same lock; a non-open latch or late confirmation denies, revokes the provider session, captures the provider user for cleanup, and never creates a profile. In one transaction an eligible open-latch completion marks the receipt `active`, creates exactly one profile with retained age attestation, `session_epoch=1`, and `sessions_revoked_before=current provider session created_at - 1 microsecond`, then registers that exact session at epoch 1. For `initial_admin`, the same transaction writes the minimal `admin_bootstrap_state.subject_user_id/receipt_id` binding with a 24-hour expiry before admission-sensitive fields purge. Exact replay returns the existing result; any mismatched receipt/profile/session or partial state denies and reconciliation resumes safely. A deletion-scheduled account that reauthenticates receives only `cancellation_only`, never a new active profile/session. Tests cover completion-versus-drain, confirmation while `draining|blocked`, and confirmation before, exactly at, and after the verification deadline/skew boundary, including response loss after successful `verifyOtp`.

`register_current_session` derives `auth.uid()` and session ID from the verified JWT, then calls a private `postgres`-owned `SECURITY DEFINER SET search_path=''` helper containing one static schema-qualified exact-row query that returns only `auth.sessions.created_at` for matching ID/user. Managed Supabase owns `auth` objects; do not change their ownership or grant custom roles direct Auth-table access. The helper remains outside exposed schemas, has no dynamic SQL, and revokes execution from `PUBLIC`, `anon`, `authenticated`, and ordinary application roles; only the private registration function owner may execute it. Missing/deleted/wrong-user row or helper error denies. Registration requires immutable provider creation time strictly after `profiles.sessions_revoked_before`; refresh preserves the original boundary. Existing revoked/cancellation-only rows never reactivate. All-device, password-recovery, deletion, and security revocation atomically advance `sessions_revoked_before` and increment `session_epoch`; older epochs and earlier/equal provider sessions deny. Application revocation commits first, then an idempotent outbox requests provider revocation. H-01 must prove helper create/call/revoke in hosted Supabase and rerun it after platform upgrades; local proof alone is insufficient. Tests include hosted privileges, spoof/search-path denial, refresh stability, deleted/missing provider row, completion crash/replay/mismatch, and unseen pre-revocation session after provider-revocation failure. Test-role mutation requires `environment_stage=synthetic_alpha`, a Synthetic Store where scoped, Administrator MFA/recent-auth, exact subject/role/store, reason, and atomic audit; the commands return `stage_disabled` in every shared/external/production stage. Production Representative grants exist only through Package 6 approved onboarding/claim transactions. `request_account_deletion` and `issue_export_download` require server-verified shopper password authentication within 10 minutes, plus MFA only when that shopper already enrolled it; privacy rights never require new MFA enrollment. Use the approved tested recovery fallback when an enrolled factor is unavailable. UI state alone never suffices. Export creates at most one active job; archive expires after seven days and each signed URL after 15 minutes. Scheduling deletion revokes application sessions/grants first and leaves cancellation-only authentication for seven days. Cancellation restores ordinary access but privileged grants remain revoked and require normal regrant/reverification; the deletion preview states this consequence.

**Initial production Administrator bootstrap:** before the first shared environment, the signed Product/Security receipt and atomic bootstrap branch of `set_account_registration_mode` above admit exactly one unprivileged account; that person completes provider email verification, `complete_account_registration`, and TOTP MFA. A deployment-only job then stores a separate 256-bit grant nonce hash and 30-minute expiry in `admin_bootstrap_state`; the raw nonce exists only in the secret manager. `bootstrap_initial_administrator` is callable only with service role from the controlled deployment job, only while `environment_stage=pre_release`, only when no Administrator exists, and only for the still-valid `active` subject binding whose verified email/MFA checks pass. It locks the singleton, creates one Administrator grant and audit event atomically, burns the nonce, clears the subject binding, sets `completed_at`, and permanently denies every later call. At 24-hour expiry the binding atomically becomes `cleanup_only`: it can never grant authority but retains exact subject/provider IDs solely for signed reset. `reset_expired_initial_administrator_bootstrap` locks that cleanup-only record, revokes sessions, deletes the abandoned unprivileged profile/provider user, clears the binding, returns registration mode to `closed`, and permits a wholly new signed bootstrap; a one-hour delay alerts and a 24-hour delay closes shared activation until cleanup succeeds. The binding is no-log/no-export, deletes immediately after successful bootstrap/reset, and ages from backups within 30 days. Service-role/nonces never enter application routes, browser, logs, or repository. Tests cover first-account admission, admission-purge/subject-binding boundary, 23:59/24:00 authority denial, reset after expiry job delay, cleanup failure/escalation/retry, signed reset/new bootstrap, wrong user, missing verification/profile/MFA, expired/replay admission or grant nonce, existing profile/Administrator, wrong stage, partial failure, and immutable receipts.

**Jobs/lifecycle:** daily inactivity evaluator uses the approved three-year + 90-day warning/deletion formula; notification delivery failure never extends it. Export builder/expiry; day-8 account deletion; Storage cleanup; deletion-receipt replay; audit partition/retention. Security response may revoke sessions or feature grants, but no undocumented general account-suspension state exists in Regional Public MVP.

**Failure/verification/rollback:** generic enumeration-resistant auth/recovery messages; interrupted export/deletion resumes idempotently; cancellation after deadline returns deletion complete. Prove session rotation, revoke-next-request, MFA/recent-auth, cross-account and wrong-role denial, atomic allowed audit, durable denied-attempt outbox, inactivity boundary dates including Feb 29, export contents/exclusions/checksum, deletion/restore replay. Stop on any session/grant bypass, audit gap, or inability to restore without resurrecting deleted data.

Package 2 additionally proves current/all-device/password-recovery/deletion/security revocation against every private table, RPC, Function, Storage/signed-URL, export, and cached-data path; wrong/missing session ID, forged registration, revoked-row re-registration, stale session epoch after every all-device path, tombstone expiry boundaries, cancellation-only escape, logout cleanup, and provider-revocation outage. Browser inspection finds no token in localStorage, Cache Storage, URL, log, or telemetry. Audit-chain mutation/gap/root/missed-anchor/restore tests pass before privileged external use.

## Package 3 â€” Shopper-private actions, New Since, and correction intake

**Outcome and scope:** saves, private 1â€“5 rating/note/visit memory, coarse catalog-last-seen, New Since, and correction reports. No sharing, trip plan, public review, or privileged correction approval.

**Routes/states:** Package 1 Browse/Details gain JIT-auth `Save`; `Add` remains absent until 5A; private memory controls; `/saved`, `/account/history`, `/stores/:slug/correction`, `/corrections/:id`. Private action states are loading/saved/updated/delete-pending/undone/deleted/error. Correction draft may start anonymously, but submit requires JIT verified-account auth; cancellation writes nothing. Correction is `submitted`, `triaged`, `resolved`, or `closed` with own reason-neutral status.

**Schema and ownership:** `saved_stores(user_id, store_id, created_at)`; `private_store_memories(user_id, store_id, rating nullable 1â€“5, note nullable bounded plain text, last_visit_month nullable, version)`; `catalog_last_seen(user_id, area_id, seen_at)`; `catalog_new_dismissals(user_id, store_id, dismissed_at)`; `store_correction_reports(reporter_user_id NOT NULL, store_id, type, bounded description, public_source_url nullable, state, assigned_admin_id nullable, version)`; `correction_case_events`. Private rows are own-only; correction workers see only assigned operational fields, not unrelated shopper data.

**Commands/jobs:** `toggle_save`; `upsert_private_memory`; `delete_private_memory`; `mark_catalog_seen`; `dismiss_new_store`; `submit_store_correction`; `update_correction_case`. Own-write retries are idempotent; memory uses expected version. A short Undo delays destructive private-row purge, then the 24-hour deletion worker removes rows. Dismissed New Since entries remain in Recently Added for 30 days; a cleanup job deletes dismissal markers after that period.

**Failure/verification/rollback:** successful JIT authentication completes the exact preserved low-risk private action and returns to context; cancel/failure performs no write. Reconfirmation is reserved for separately named high-risk/destructive actions. Prove User A/User B isolation, Representative/Admin denial, invalid rating/text/URL limits, action Undo, stale-write conflict, anonymous correction write denial, account/IP/device rate limiting, own-status/minimal queue disclosure, New Since area/30-day behavior, export/deletion. Rollback hides new actions first and preserves private rows for compatible re-enable; a privacy defect requires disablement and scoped purge/receipt.

## Package 4 â€” Candidate Link and Candidate Share

**Outcome and scope:** private manual/URL candidate capture, one-recipient verified-email share, recipient-owned Trip Idea, block/unblock/report. No unregistered delivery, public listing publication, household grant, social scraping, or automatic event/store creation.

**Routes/states:** `/capture`, `/shares`, `/shares/:shareId`, `/trip-ideas`, privacy blocked-sender control. Capture is `manual_draft`, `extracting`, `needs_review`, `saved`. Share sender state is only `Pending`, `Accepted`, `Closed`; recipient action is Accept/Dismiss/Block/Report; all nonaccepted terminal reasons conflate.

**Schema and ownership:** `candidate_links(owner_user_id, normalized_url nullable, destination_host nullable, title, note, provenance, extraction_state, version)`; `candidate_shares(sender_id, recipient_id server_resolved nullable, recipient_email_hmac, key_version, state, expires_at, accepted_at, version)`; encrypted/private `candidate_share_payloads`; `candidate_blocks(blocker_id, blocked_user_id, created_at)` after resolution; `trip_ideas(owner_user_id, source_share_id nullable, title, url_note, version)`; `candidate_abuse_cases` containing only opaque parties, destination-host HMAC, reason, timestamps, and minimum reported text. Full URL/path/query/fragment never enters a case, log, email, audit, export, or receipt. Sender cannot read match/block/reason; recipient cannot read sender-private records; Administrator has no default access.

**Server commands/jobs:** `extract_candidate_url` uses SSRF controls and allowlisted response limits; `save_candidate`; `send_candidate_share`; `accept_candidate_share`; `close_candidate_share`; `block_candidate_sender`; `unblock_candidate_sender`; `report_candidate_share`; `revoke_candidate_share`; own-only `update_trip_idea`; own-only `delete_trip_idea`. Send always returns the approved generic 202 envelope no earlier than 500ms and uses identical shape across match states. Acceptance transaction creates recipient-owned copy and deletes shared-payload access. Editing uses expected version and preserves safe values on conflict. Deletion requires explicit confirmation, removes the idea from primary data within 24 hours, ages it from backups within 30 days, and retains only the standard 31-day content-free deletion receipt; it does not reopen or alter the sender's closed share. `Add to Trip` is absent until Package 5A. Expiry/terminal cleanup, accepted-envelope 30/90-day cleanup, block/account deletion, and moderation retention follow approved rules.

**Failure/verification/rollback:** extraction failure preserves manual draft; private/blocked sources fall back without bypass. Prove URL scheme/DNS/redirect/size/type SSRF denial, 100-trial timing test per state with median difference <50ms, rate limits, replay/idempotency, sender/recipient isolation, no email to unregistered address, terminal payload deletion, Trip Idea edit/conflict/delete/lifecycle/export, Package-4 absence of Add to Trip, block/unblock, report evidence minimization, export/deletion. Disable send/extraction separately on abuse/provider failure; existing recipient-owned ideas remain private.

**Executable fetch limits:** URL â‰¤2,048 bytes; HTTP/HTTPS ports 80/443 only; â‰¤3 redirects; 2-second connect/6-second total timeout; â‰¤1 MiB compressed/2 MiB decompressed; `text/html|text/plain`; â‰¤64 KiB extracted text; no subresources. Validate all A/AAAA addresses, pin a public IP with Host/SNI, and re-resolve/re-pin redirects. Strip auth/cookie/proxy-auth/Origin/Referer. Fetcher has no database/Storage/internal/metadata route. Rate 10/account/hour, 30/IP/hour, 5/account/host/hour, 2 concurrent/account. If runtime cannot pin destination, external extraction stays disabled. Test IPv4/IPv6 alternate forms, mixed DNS, rebinding, redirect-private, metadata, slow/loop, MIME lie, compression bomb, oversized/malformed content, log leakage, and manual-draft preservation.

## Package 5A â€” Manual Trip, collaboration, Go, and offline

**Outcome and scope:** manual trip creation/order, one invited partner, readiness warnings from hours only, Creator/Partner/Navigator rules, active Go, external Google Maps/Waze leg handoff, visit memory, encrypted offline active-trip recovery. No suggested ordering, discovery map, travel-time provider call, or route optimality claim.

**Limits/routes/states:** maximum eight active stops per trip. Routes are `/trips`, `/trips/new`, `/trips/:id/invite`, `/trip-invitations#token=...`, `/trips/:id/plan`, `/trips/:id/go`, `/trips/:id/summary`. Trip is `draft`, `ready`, `active`, `completed`, `cancelled`; stop is `planned`, `arrived`, `completed`, `skipped`, or `observed_closed`; invitation is `pending`, `accepted`, `revoked`, `expired`; participant is `creator` or `partner`; exactly one active Navigator account/device during Go. Package 5A action is `Review Hours`, states `Travel time is not included`, and makes no arrival/finish/feasibility/suggested-order claim.

**Schema and ownership:** `trips(owner_id, area_id, name, local_date, departure_local_time nullable, start_kind nullable manual|current_location, private_start_label nullable, private_start_lat/lng nullable, return_label/lat/lng nullable, max_drive_miles nullable 1â€“500, max_total_minutes nullable 30â€“1440, state, navigator_user_id/device_binding nullable, version)`; `trip_stops(trip_id, kind store|rest, store_id nullable, rest_label/address/lat/lng nullable, position, priority must|prefer|flexible, planned_dwell_minutes default 60 constrained to whole minutes 5â€“720, state, arrived_at/completed_at/closed_observed_at nullable, version)`; `trip_invitations(token_hash, recipient_email_hmac, expires_at, state)`; `trip_participants`; `trip_device_bindings`; `trip_mutation_receipts(idempotency_key, base_version, device_id, local_sequence, result)`; `trip_visit_memories(author_user_id, trip_id, store_id, rating nullable 1â€“5, return_choice nullable no|maybe|yes, note nullable, version)` own-author only. `name` is required, trims/collapses whitespace, rejects control characters, and is 1â€“80 Unicode code points; creation proposes `[Area name] Antique Day`, which the user may edit before submit. Date is required at creation. Draft and Package 5A `Review Hours` permit every start/departure field to remain null. Package 5B `check_my_day` and Package 5A `start_trip` each transactionally require departure time, a valid start kind, and fields required by that kind; Package 5B additionally requires provider-dependent inputs. A store stop requires `store_id`; a rest stop requires a private label/address and has no store-hours claim. `must` is the required-stop value. The UI offers 30/45/60/90-minute presets plus Custom whole minutes from 5 through 720 and permits either/both maximums. Start/return and exact rest-waypoint addresses/coordinates are private, never logged/emailed, deleted from primary within 24 hours after completed-trip sync, omitted from export after purge, and age from backups within 30 days; only an optional coarse user label may remain. Creator/accepted partner read/edit one trip; only Navigator/device mutates Go; every visit-memory field remains author-only.

**Commands/jobs:** `create_trip`; `rename_trip`; `set_trip_start`; `set_trip_return`; `set_trip_limits`; `add_trip_stop`; `add_rest_stop`; `remove_trip_stop`; `reorder_trip_stop`; `set_trip_stop_priority`; `set_trip_stop_dwell`; `update_trip_schedule`; `invite_trip_partner`; `accept_trip_invitation`; `revoke_trip_invitation`; `leave_trip`; `assign_navigator`; `bind_navigator_device`; `transfer_navigator_device`; `start_trip`; `mark_arrived`; `complete_trip_stop`; `skip_trip_stop`; `mark_trip_stop_closed`; `restore_trip_stop`; `complete_trip`; `replay_trip_mutations`. `rename_trip` is available to Creator/accepted Partner while draft or ready, validates the same name boundary, and requires expected trip version/idempotency; stale rename exposes only the shared latest name and uses Reapply/Keep Latest. `mark_trip_stop_closed` moves planned/arrived to terminal observed-closed, records the observation, and triggers the same remaining-plan recalculation as Skip; Restore returns it to planned and removes the observation. All transitions participate in versioning, idempotency, offline replay, history, and tests. Fragment tokens exchange once and scrub before third-party requests. Draft writes use expected version; stale UI highlights server diff and offers Reapply/Keep Latest. Invitation expiry at seven days; accepted participation follows trip lifetime; completed-trip coordinate purge follows approved 24-hour/30-day backup rule. Tests cover generated/edit creation name, 1/80/81-code-point and control/whitespace boundaries, owner/partner rename, wrong-user denial, stale conflict, replay, and export/history display.

**Offline/failure:** `start_trip` issues a signed offline grant bound to account/trip/Navigator device/session-security version/device key for at most 36 hours; it never authorizes the server. Only the matching local Auth subject decrypts; otherwise show only that an offline trip exists. Clear plaintext after 15 minutes backgrounded. At 36 hours lock display/mutation pending sync; matching online reauth within seven days may replay/purge, then purge on next execution. Clock rollback over five minutes locks until online. Account switch/known revoke/confirmed logout purges immediately. No authenticated trip data enters Cache Storage. Ordered replay rechecks current application session, trip grant, Navigator/device, trip state, version, and idempotency; same-author note/rating/return-choice conflict preserves both versions for explicit choice. External handoff uses encoded current-stop address and user tap; missing app falls back to HTTPS. Prove 36-hour/7-day/background/clock boundaries, cold/wrong/no-account restart, every stop including observed-closed/Undo/last-stop Summary, partner leave/remove, Go pause, device transfer, replay exactly once, denied old device, all location purges, cross-account denial, eight-stop bound, and accessibility.

## Package 5B â€” Secondary Browse map and Check My Day

**Geocoding contract:** R-01 must select and test geocoding as well as routing. Send only the user-entered start/return/rest place text needed for the request; disclose no account, trip/store ID, note, or precise device location. Show a bounded candidate list with readable address/context and require explicit user confirmation before saving coordinates or routing. Ambiguous/no-result/provider-failure states preserve the original text and offer Edit, Retry, choose a store/manual map link, or continue with Package 5A hours-only review. Never silently choose the first candidate. Store the confirmed label/coordinates privately under the approved location lifecycle; test similarly named/wrong-city results, accessibility, quota, timeout, retention, correction, and provider outage.

**Outcome and prerequisite:** add a secondary accessible Browse map and deterministic suggested feasible order after the accepted mapping/routing ADR names provider/version/plan, data/retention/region, auth, attribution, quotas/cost caps, timeout/retry, legal terms, outage fallback, and contract fixtures. Browse list and Package 5A manual order remain fully usable without provider.

**Map contract:** `get_browse_map(bounds, zoom, q, category, open_now, visited, saved, claimed, max_area_centroid_miles)` returns at most 500 approved public store points plus server-derived public/private flags; more matches return `too_many_results` and require zoom/filter rather than truncating silently. Distance is from the selected area's public centroid, never device location. The provider receives public coordinates only; authenticated saved/visited flags remain in Antique Trail results and are not sent to it. Use provider-native clustering below street zoom and expose each cluster count/zoom action to keyboard and assistive technology. Unclustered marker/preview includes store name, public rating/count, today's hours/open state, claimed status, category, approximate area-centroid distance, saved/visited state when authorized, and `View Details`, JIT-auth `Save`, `Add to Trip`, and `Navigate`. Category, open-now, visited/unvisited, saved, claimed, distance, state/region, text, and area filters apply server-side before the bounded response. The synchronized result list remains the complete accessible control. An explicit `Search this map area` changes bounds; panning alone does not. Show provider attribution. Timeout/quota/blocked script hides map with a plain message and preserves list, filters, focus, and Back behavior. No provider response enters service-worker cache.

**Suggestion algorithm:** input is one to eight store/rest stops, private start/departure, optional return, each whole-minute dwell 5â€“720, must/prefer/flexible priority, optional maximum drive miles/total minutes, verified store hours, and provider distance/time matrix including start/return. Departure time is at the private start; start-to-first travel determines first arrival. Each subsequent leg starts after prior dwell plus 10-minute transition buffer. When return exists, last-to-return travel contributes to distance, duration, limits, estimated finish, and scoring; without return, finish is the last departure. Rest stops are always-available private waypoints and create no open claim. Reject stale/unknown store hours as warnings, never invent them. Enumerate every stop permutation and select by this ordered score: prefer routes satisfying every set maximum; maximize `must` stores completed by closing; maximize priority points completed by closing (`must=3`, `prefer=2`, `flexible=1`); maximize all stores completed by closing; minimize summed proportional excess across any unmet maximums; minimize total late minutes; minimize travel minutes including boundary legs; preserve more original adjacent pairs; lexicographic stable stop-ID sequence tie-break. Waiting before opening is allowed. Return selected order, per-stop arrival/departure/warning, boundary/stop travel distance/time, limit result, estimated finish, matrix timestamp, and reasons for priority/required/constraint placement. The algorithm never drops a stop. The UI shows `Use Suggested Order` and `Keep My Order`; neither applies automatically and neither claims real-world optimality. Fixtures cover start/return boundary legs, no-return, 5/720-minute dwell, every priority, both maximums, rest stops, and eight stops.

**Schema/commands:** `provider_route_runs(trip_id, requested_by, provider_contract_version, input_digest, matrix_generated_at, status, cost_units, expires_at)` stores no raw precise start/return coordinates; `route_suggestions(run_id, ordered_store_ids, explanation, warnings, expires_at)`; `map_provider_health`. `get_browse_map`; `check_my_day`; `apply_suggested_order`. Provider call sends only required coordinates; exact user start/return exists in memory for the call and is purged within 24 hours after sync. Timeout is 8 seconds with one idempotent retry only for safe transient failures; then `provider_unavailable` and manual order remains.

**Verification/rollback:** provider contract tests, minimized payload/log inspection, quota/rate/cost alert, deterministic fixture/tie tests, DST/hours/stale/closed/impossible cases, apply conflict, list fallback, keyboard/zoom/screen reader, eight-stop worst-case performance, timeout/outage. Disable map and suggestion capabilities independently; never disable list/manual plan.

## Package 6 â€” Partner onboarding and Store Portal

**Outer-access admission:** before any Private-Beta QR is generated, H-01 requires the Administrator to record the verbally confirmed owner email/consent and create one expiring Vercel Deployment Protection access grant, using an independently accepted enrollment mechanism, covering every onboarding/Auth callback hostname/path. Hosting identity is only the outer network boundary and grants no application user/role/store scope. Wrong/expired identity denies before the app; removing the hosting grant does not replace application/session/grant revocation. Synthetic local Package 6 needs no hosting grant. If the selected Vercel plan cannot provide bounded participant enrollment without exposing another hostname, Private Beta remains blocked.

**Outcome and scope:** phone-first QR/fragment pilot onboarding; application-atomic provisional consent/pending identity followed by separate Supabase Auth binding; owner-controlled draft; authority verification; Synthetic claim workflow; exact one-store grant after approval; direct/controlled store text/hours/social/support. Official media and screenshots remain placeholder/disabled until M-01. No self-approval, shopper-private access, public reviews, analytics, claim document upload, multi-store Representative, or multiple active Representatives for one store.

**Prerequisites/stage:** E-01 is required before real verification/recovery/status email; M-01 before any real owner image/screenshot upload; H-01/S-01/HC-01 before external use. Build claim routes with Synthetic data, but keep `public_listing_claims_enabled=false` in Alpha, Private Beta, and Package 10A: route not found, reads empty, writes stage-disabled. Package 10B alone enables claims. A failed media gate keeps placeholders and blocks any gate requiring owner media; it never publishes unscanned content.

**Routes/states:** `/partner/join#token` â†’ scrubbed `/partner/join` â†’ `/partner/verify` â†’ `/partner/draft` â†’ `/partner/status` â†’ approval status/email â†’ normal sign-in with verified email and MFA â†’ `/partner/activate` tasks 4â€“5 â†’ `/store-portal`; plus Store Portal and claim routes in `DESIGN_SYSTEM.md`. Invitation is `active|registration_pending|consumed|expired|revoked`; pending identity is `provisional|auth_pending|bound|expired`; onboarding is `draft|submitted|changes_requested|approved|rejected|withdrawn`; claim is `draft|submitted|verification_pending|changes_requested|conflict|approved|rejected|withdrawn|revoked`. One readable 320px task/screen, no auto-advance/countdown, safe draft preservation, and generic token/wrong-account failure are required.

**Schema and ownership:** `partner_invitations(token_hash, recipient_email_hmac, admission_receipt_id nullable, created_by, expires_at, state)`; `pending_partner_identities(email_hmac, key_version, auth_user_id nullable unique, state, expires_at, version)`; immutable provisional consent/receipts; pilot drafts; authority checks; partnerships; `listing_claims(claimant_id, store_id, state, risk_tier, version)`; content-free `claim_authority_signals(channel_class, type, status, verified_by/at)`; claim events; Store Portal hours/changes/updates/social/support tables. Partial unique indexes enforce one active/submitted claim per claimant/store, one approved active claim/store, one active grant/store, and one active store grant/Representative. Claimant sees own status/safe submission only; assigned Administrator sees the exact case/signal allowlist; no sibling/bulk evidence.

**Onboarding commands/lifecycle:** invitation creation uses the verbally confirmed exact owner email and stores its HMAC on the pending invitation; it does not pre-create an admission secret. `consume_partner_invitation` accepts the token, acknowledgements, typed identity, and normalized emailâ€”but never password/MFAâ€”and in one application transaction burns the parent into `registration_pending`, matches the confirmed email HMAC, writes provisional consent, creates one unprivileged pending identity, and for a new user issues/returns the Package 2 `purpose=partner` child registration URL by the raw-secret handoff contract. Existing Auth users sign in normally and no child is issued. The parent never replays. Lost unused child recovery requires Administrator repeat verification, revocation of that pending child/identity, and a new invitation. `bind_partner_identity` derives verified email/user from current Auth, requires MFA, locks/matches the email HMAC, binds one user once, marks the onboarding bound, and finalizes the receipt transactionally. Interruption remains resumable/unprivileged. A failed/expired/revoked invitation also revokes an unused child; Package 2 owns retry/orphan cleanup. Orphan Auth accounts have shopper privilege only. Unbound records expire after 30 days; typed PII/provisional consent deletes within 24 hours, leaving content-free outcome. Only final binding permits draft access. Other commands: draft save/submit, authority check, approve/reject/request changes/withdraw. Tests prove wrong-email, atomic parent burn, old-parent replay denial, lost-child new-invitation recovery, admission-receipt mismatch/revoke, existing/new account, every cross-system retry boundary, and no role/store scope before final approval.

**Claim commands/lifecycle:** start/submit signals/submit/request changes/conflict/approve/reject/withdraw/revoke/transfer. Require verified email, MFA, exact store, and two independent distinct channel classes/events: business-domain/published-contact response plus callback, mailed code, filing lookup, or in-person inspection. Same email/phone/contact cannot count twice. Lease/utility may be viewed in person; store no copy. Approval locks the authority root and every claim/grant, rechecks evidence/conflicts, creates exactly one grant and conversion event atomically; existing grant returns conflict. Transfer atomically revokes old exact scope before new approval/grant. Annual/risk reverification failure revokes exact scope. Keep content-free verification/grant history three years; there is no raw digital claim evidence lifecycle because upload is absent.

**Portal/media/support:** direct/controlled field, text update, social link, and support commands remain as approved. Real media commands and screenshot upload do not exist until M-01 proves MIME/magic, pixel/byte/decompression limits, isolated decode, malware scan failure-closed, safe re-encode, EXIF/GPS removal, private quarantine, derivative-only review, authorized five-minute URLs, cleanup, and DB/Storage restore. Until then use neutral placeholders and text-only support.

**Verification/rollback:** test every onboarding boundary, existing/mismatched/changed email, duplicate/replay/concurrent bind, orphan Auth, no credential/token leakage, and no draft/grant before binding. Test claim stage-off on route/RLS/RPC/Storage/job, two-admin concurrency, same store/two claimants, one claimant/two stores, duplicate/transfer/rollback, signal independence, self/sibling/bulk denial, revoked open session, and zero-or-one active grant after every failure. Test direct/controlled text fields, social validation, support scope, withdrawal/regrant, HMAC rotation, audit, and media-command absence. Break-glass remains disabled until its reviewer gate passes.

## Package 7 â€” Administrator review, Access & Safety, and duplicate merge

**Outcome and scope:** typed review queue; partner/store/image/support decisions; exact-scope grant/revoke/regrant; narrow D30 View Audit; reversible duplicate merge. No shopper-private browsing, full D31 search/export, bulk approval, or routine public-review moderation.

**Schema/commands:** `admin_review_cases` with typed target/snapshot/state/lock/version; `admin_case_events`; `merge_ledgers`; `store_tombstones`; existing grants/audit. Commands: `claim_review_case`; `request_case_changes`; `approve_case`; `reject_case`; `revoke_store_scope`; `regrant_store_scope`; `view_resource_audit`; `preview_duplicate_merge`; `execute_duplicate_merge`; `rollback_duplicate_merge`. Initial production Representative grants are impossible here and exist only through Package 6 approved onboarding/claim transactions. Regrant requires a prior revoked exact-scope grant plus current subject verified email/MFA, current authority evidence, approved onboarding/claim state, Administrator MFA/recent-auth, exact scope preview, reason, and audit; a direct call missing any prerequisite denies. Case lock expires after 15 minutes and never authorizes action by itself.

**Case scope:** queue projection contains metadata only. Opening an assigned case grants no standing access; each evidence query rechecks role/session, assignment, exact target/store/type, and an approved field allowlist. Reporter identity, fraud signals, sibling cases, other claim/support evidence, shopper history, and bulk evidence export are denied unless a separately approved incident capability names that exact field/scope. Selected-case allow and every sibling/bulk deny path are package tests.

**Duplicate merge:** preview enumerates every reference and collision. Execution reparents allowed references atomically, preserves private authorship/visibility, creates tombstone/ledger, and quarantines/revokes every noncanonical/conflicting claim/grant; it never reparents active authority. Duplicate saves collapse to one row using the earliest `created_at`. If one user has memories for both stores, keep the canonical-store memory active and preserve the other unchanged in an own-only `private_memory_merge_conflicts` record until that user chooses which rating/note to keep or manually combines them. If one author has active reviews for both, keep the canonical review active and move the other to private `merge_conflict_hidden`, remove its old aggregate effect transactionally, and require the author to choose which becomes the one active canonical-store review; no text is combined automatically. Multiple stops inside one trip keep their positions, point to the canonical store, and receive a private duplicate-stop warning until the trip owner removes one; readiness metrics count that store once per trip. Every collision choice/state and aggregate delta is in the merge ledger. Replacement access requires normal reverification/new grant. Rollback restores original IDs, states, aggregates, and collision rows but never silently reactivates access.

**Verification/rollback:** type-specific previews/actions, no payload editing, required reasons, stale lock/version, self-approval denial, Representative open-session denial after revoke, narrow audit, no shopper-private queries, exact-case/sibling/bulk denial, atomic merge failure, redirect, rollback, grant quarantine/reverification. Disable each privileged command on audit/outbox/root-anchor failure; no â€œpartial success.â€

## Package 8 â€” Synthetic Internal Alpha and External Testing Readiness

**Outcome:** after H-01 has protected every shared hostname and proven Alpha recovery, prove Packages 1â€“7/5B together with Synthetic data. L-01 must pass before any shared privileged mutation; until then, privileged cycles are local-only. Then prove E-01/M-01/S-01/HC-01 and all owner-contact prerequisites. It creates no public capability and authorizes no owner contact until every gate passes.

**Evidence model:** `environment_stage(stage, capabilities, changed_by, receipt_id)` is server-owned; `test_run_receipts`; `release_gate_receipts`; `evidence_responsibility_grants(user_id, responsibility ProductOwner|PrimaryInternalTester|Engineering|Security|Operations, state, source_receipt_id, version)`; one-use `gate_signing_capabilities(token_hash, user_id, responsibility, gate_kind, frozen_digest, expires_at, state)`; signed content-addressed evidence manifest containing artifact hashes/links, not private payloads. Evidence responsibility is not an application role and grants no catalog, shopper, store, case, or admin data authority. The same Product+Security bootstrap receipt may let the deployment service grant the verified initial Administrator's MFA account the first `ProductOwner` and `PrimaryInternalTester` responsibilities; every later grant/replacement requires the signed HC receipt. Neither path is callable by an application Administrator, and Administrator alone never suffices. After evidence freeze, the trusted service issues a 30-minute exact-digest decision capability whether checks pass or fail. `Pass/Sign` is permitted only when every gate predicate passes; `Reject` is always permitted and must bind immutable failed-check codes/reasons. Decision derives current user, exact gate-required responsibility, MFA/recent-auth, capability, digest, expected version, and decision; same-input replay returns the receipt and any mismatch denies. Revoke responsibility on HC replacement or account deletion; keep content-free grant history three years after responsibility ends, delete capability hashes within 24 hours after use/expiry and from backups within 30 days, and exclude both from shopper export/logs. Internal Alpha stage is `synthetic_alpha`; real-store predicates deny. Break-glass stays disabled through Package 8/8B for a sole Administrator; a second qualified Administrator may satisfy its separate approval rule. Package 9 owns the independent-reviewer credential path. Tests prove bootstrap/later grant boundaries, ProductOwner versus PrimaryInternalTester gate mapping, responsibility-only frozen-packet read/decision, pass-on-failure denial, authenticated rejection, Administrator-without-responsibility denial, no expanded data read, expiry/revoke/replay/lifecycle, and immutable receipt.

**Execution:** deterministic reset; Scott uses separate Test User A, optional Agent-Assisted Shopper, Representative, and Administrator accounts; the newly created Test User B belongs only to the Independent Internal Tester on her phone and is never a reassigned solo-stage account. `Synthetic only` globally means every store, listing, trip content, review, claim, update, support case, and media object is fictional; it does not prohibit the approved minimum real internal-tester account allowlist needed for separate control: verified email, Auth user ID, age-18 attestation, MFA/recovery state, consent timestamp, device/browser class, and test results. Do not store precise location, private household information, or unrelated identity data; redact email/user IDs from evidence, provide export/deletion, and delete the tester account/evidence linkage within 30 days after the signed stage disposition unless the tester chooses continued participation. This never permits an external owner, real store, or public audience. Run required shopper and privileged cycles, browser/accessibility/older-adult preparation, DB and Storage backup/restore, rollback, incident rehearsals, monitoring/error/support/status/on-call exercises, legal pilot terms, and operating entity/insurance evidence. AI may execute/collect but cannot sign.

**Exit/stop:** Product Owner/Primary Internal Tester signs only with zero Blocking/privacy/security/data-loss defect and every dated artifact. Monitored support/security contacts, response commitments, on-call/backup, and PWA/status path must work. A failed check, missing hash, real store/owner/external-participant data, internal-tester data outside the approved minimum allowlist, shared role session, or unapproved external participant blocks and returns stage to the last safe state.

## Package 8B â€” Three-store Controlled Private Beta

**Outcome and bounds:** exactly one verified Partner/Pilot Store at a time, maximum three, invitation-only. No advertising, anonymous real-store access, public reviews/UGC, or automatic next-store admission.

**Schema/authorization:** `pilot_cohorts`; `pilot_visibility_grants(user_id, cohort_id, expires_at, revoked_at)`; `pilot_store_admissions(store_id, ordinal 1..3, gate_state, receipt_id)`; `pilot_gate_receipts`. Only invited verified accounts with active cohort grants read active Pilot Store Records. Representative sees assigned store. Grant revocation/expiry denies next request.

**Execution/evidence:** add store ordinal N only after Nâˆ’1 passes consent/authority/onboarding, data accuracy, direct/controlled publishing, support, shopper-trip, authorization/audit, recovery, incident, withdrawal, and 100% current verification checks. Before store ordinal 2, the first owner independently records `continue` or `withdraw` and whether the listing is useful, hours maintenance is understandable, one direct edit and one reviewed change were completed, each flyer/social channel is accepted or declined, and the operator minutes/interventions/support cases required. `Withdraw` or missing evidence blocks ordinal 2; `continue` still requires Product Owner acceptance of the recorded support load. Product Owner signs each receipt. After store 3, stop and open Regional Public Readiness; passage never enables public access.

**Rollback/stop:** withdraw/hide exact store, revoke exact grants, preserve approved audit/consent retention, notify cohort, and restore prior stage. Any scope leak, public route, owner implication, or unapproved fourth store is Blocking.

## Package 9 â€” Public reviews, moderation, and appeal

**Outcome and stage:** implement the complete text-only review system and narrow independent-reviewer WebAuthn service with Synthetic records while `public_reviews_enabled=false`. Package 10B alone enables public reviews after readiness signature. Shopper/review photos and owner responses stay absent.

**Schema and ownership:** `public_reviews(author_id nullable ON DELETE SET NULL, store_id, rating, text, display_name, visit_month, eligibility_kind, conflict_kind, state, version)` unique active author/store while author exists; `review_versions` with restricted text; `rating_aggregates(store_id, eligible_count, rating_sum, version)`; `review_reports`; `moderation_cases`; `moderation_case_evidence`; `review_appeals`; `reviewer_identities`; `reviewer_webauthn_credentials`; `reviewer_setup_capabilities`; `reviewer_management_capabilities(token_hash, reviewer_identity, expires_at, state)`; `appeal_reviewer_capabilities(token_hash, reviewer_identity, case_id, expires_at, state, packet_hash)`; `break_glass_cases`; allowlisted `break_glass_review_packets`; `break_glass_reviewer_capabilities(token_hash, reviewer_identity, case_id, packet_hash, expires_at, state)`; immutable `break_glass_review_receipts`; append-only hash-chained case/audit events. Public projection exposes only approved fields. Author owns edit/delete/appeal; Representative reports/appeals exact store but cannot review own store; Administrator has case-scoped routine moderation only.

**Commands:** `create_review`; `edit_review`; `delete_review`; `undo_delete_review`; `report_review`; `moderate_review(action Hold|Remove|Restore|Dismiss Report)`; `submit_review_appeal`; `assign_appeal`; `exchange_appeal_capability`; `decide_appeal(Restore|Uphold)`; `rebuild_rating_aggregate`; plus reviewer `issue_reviewer_setup_capability`; `issue_reviewer_management_capability`; `begin_reviewer_webauthn_enrollment`; `finish_reviewer_webauthn_enrollment`; `begin_reviewer_assertion`; `finish_reviewer_assertion`; `list_reviewer_credentials`; `revoke_reviewer_credential`; `begin_reviewer_recovery`; `complete_reviewer_recovery`; and break-glass `request_break_glass`; `approve_break_glass`; `activate_break_glass`; `expire_break_glass`; `issue_break_glass_review_capability`; `exchange_break_glass_review_capability`; `submit_break_glass_review(Compliant|Exception)`. Delete removes public/aggregate effect and enters `delete_pending` atomically; author-only Undo lasts exactly 60 seconds and restores only if eligible/not held/removed. At expiry, finalize and purge text within 24 hours. Review/aggregate/audit change transactionally. Account-deletion scheduling hides/removes aggregate immediately. Day 8 purges display name and all text versions, nulls `author_id`, deletes author linkage in versions/reports/appeals, and replaces any content-free retained metadata key with an irreversible random tombstone not derivable from user ID. Only minimum evidence already copied to a live restricted case may retain a case-scoped subject reference; restore replay reapplies the de-identification before access.

**Independent reviewer foundation and appeal:** Package 9, not Package 2/SLM-01, owns this capability. A Product Owner-signed named-reviewer identity/confidentiality receipt lets a trusted service issue one hashed 30-minute setup capability. `/reviewer/setup#token` requires exact HTTPS origin/RP, user verification, and two non-discoverable credentials before activation; one completed credential resumes only under the same unexpired capability. Credential management has no username lookup or normal session: after repeat Product Owner-verified identity, the trusted service returns one hashed ten-minute management capability through the authenticated channel. `/reviewer/credentials#token` scrubs/exchanges it, derives the exact reviewer and `allowCredentials`, requires a fresh assertion, then permits list/revoke only until capability use/expiry. Fewer than two active credentials disables case exchange. Lost-device recovery requires repeated Product Owner-verified identity proof, revokes all old credentials/live capabilities, and uses one-use `/reviewer/recover#token` to enroll two credentials; no email fallback. Exact screens/states/focus live in `DESIGN_SYSTEM.md`; every result is hash-chain audited without challenge/token/private key/biometric data. Setup/management/recovery capability hashes and challenges delete within 24 hours after use or expiry and age from backups within 30 days; none enter logs or export. Active credentials exist only for the reviewer relationship. On credential revocation or relationship end, delete public key, transports, counter, and direct reviewer linkage; retain only an environment-keyed credential-ID HMAC for 90 days to prevent immediate reuse, then delete it. Qualification and case-decision audit follows the two-year privileged-case rule, then de-identifies. Reviewer artifacts are excluded from shopper/ordinary privileged export and are disclosed only through a separately verified reviewer privacy request. Tests cover unauthorized issuer, management identity/channel/capability, no typed/discoverable lookup, wrong reviewer/origin/RP, expiry/replay/resume/cancel/unsupported, duplicate/absent verification/cloned counter, fewer-than-two disablement, recovery, relationship termination, lifecycle purge/backup aging/export exclusion, case scope, and no normal sign-in. For appeal, use a different Administrator when available; otherwise the enrolled qualified reviewer makes a fresh assertion before trusted service issues one hashed single-case capability expiring at decision or 24 hours. Packet allowlist/exclusions follow `SECURITY_AND_TRUST.md`; original moderator is denied; service applies signed decision, expires grant, and audits packet hash/result.

**Independent break-glass review:** sole-Administrator break-glass remains disabled until the Package 9 reviewer above is active and assigned before the incident. `request_break_glass` records confirmed incident ID, reason, exact field/subject/time scope, read-only default, and 30-minute expiry. A second qualified Administrator uses `approve_break_glass`; otherwise `activate_break_glass` requires the preassigned reviewer and creates only the case/access windowâ€”never a review packet or capabilityâ€”and persists `access_expires_at` plus provisional `review_due_at=access_expires_at+24 hours`. Explicit early close or automatic expiry atomically closes access, sets `review_due_at=min(existing, closed_at+24 hours)`, and enqueues idempotent packet freeze. The freeze job captures the complete hash-bound audit range including queries, record counts, allowed/denied results, closure/expiry, notice status, and external audit root, then in one transaction stores the digest, sets `review_pending`, and creates a capability expiring at the already-fixed `review_due_at`; authenticated delivery retries separately. Review exchange/submission before packet freeze denies. An expired/closed case unfrozen for five minutes disables further break-glass, raises highest-severity alert, and keeps retrying freeze/capability issuance without extending `review_due_at`. Missing review at the fixed deadline or any `Exception` also disables further break-glass. The capability exposes only the allowlist in `SECURITY_AND_TRUST.md`, never raw unrelated/private data. After fresh WebAuthn assertion the reviewer submits `Compliant|Exception`, plain reason, and packet hash. Capability deletes within 24 hours after decision/expiry; case/receipt follows two-year privileged retention then de-identifies. Tests cover disabled pre-Package9, second-admin and sole-admin paths, assignment-before-activation, no early review, exact complete packet/range/freeze, worker outage and crashes at close/freeze/capability/delivery, five-minute watchdog, fixed deadline nonextension, authenticated delivery, 30-minute access expiry, 24-hour late/missing/exception escalation, notice, replay/revoke, no routine navigation, and hash-chain/external-anchor failure.

**Nonpublic human dress rehearsal:** before close, use Synthetic Stores and people to prove eligible create/edit; ineligible and own-store denial; another user and exact-store Representative report without private disclosure; Administrator Hold/Remove/Restore/Dismiss with reasons; aggregate arithmetic; delete/Undo boundaries at 59/60/61 seconds and text purge; a different reviewer appeal; understandable notices/failures/retry/audit. Public routes remain unavailable anonymously. Any failure keeps `public_reviews_enabled=false`.

**Jobs/tests/rollback:** validation/rate/abuse queue; 14-business-day appeal target alert; case retention; text purge; aggregate reconciliation. Prove stage absence at route/RLS/RPC, eligibility/conflicts, one active review, arithmetic aggregate, edit/delete/account delete, report privacy, reason codes, hold/remove/restore, different reviewer, capability scope/expiry/revoke, author/Representative privacy, all denials. Rollback disables capability and preserves restricted case/audit data; aggregate mismatch or evidence exposure blocks enablement.

## Package 10A â€” Controlled Regional Readiness Evidence

**Readiness routing exceptions:** after R-01 is accepted, a trusted Package 10A job may make one bounded evidence call per frozen itinerary containing only required public business coordinates. An active readiness-cohort user may also explicitly invoke `Check My Day`; that call may send the selected business coordinates and only an optional manually confirmed start/return coordinate required by R-01. External-map Go handoff sends only the current business destination; Antique Trail supplies no start and lets the external map app use its own location permission. No call sends internal listing ID, audience/cohort/grant/partner status, user/account/email, private note, campaign code, or evidence identifier, and Antique Trail logs no coordinate payload. Results stay in the restricted trip/evidence scope and never create an anonymous/public provider projection. Test explicit action/consent, active-grant requirement, exact payload allowlists, wrong-area/unauthorized coordinate denial, Google/Waze URL contents, quota/fallback, revocation, and R-01 deletion/retention.

**Measured regional catalog scale:** Step 0 records expected active listing count and query plans. Add Unicode/accent-insensitive indexed search, Open Today/Open Now/freshness filters, and revision-bound 50-row pagination only when the verified active catalog can exceed 50 or measured Package 1 queries miss the performance target. If added, apply every filter before pagination, use deterministic name/UUID order, validate query/revision-bound cursors, restart plainly on revision change, and prove no duplicate/omitted row across at least the forecast count plus 25% headroom. Otherwise preserve the simpler bounded query.

**Outcome and bounds:** collect reproducible Topeka density, itinerary, independent-shopper, accessibility/browser, security/legal/support/recovery/incident evidence without public access or advertising.

**Catalog and acquisition preparation:** `regional_baseline_shops`; `listing_source_facts`; `listing_fact_reviews`; `readiness_cohorts`; `readiness_invitations(token_hash, recipient_email_hmac, admission_receipt_id nullable, expires_at, state pending|registration_pending|accepted|revoked|expired)`; `readiness_visibility_grants(user_id, cohort_id, expires_at, revoked_at)`; `readiness_metric_events`; `regional_readiness_receipts`. Operations may enter only manually verified public business facts after two-person source/provenance review. Approved readiness listings carry `audience=regional_readiness`, never `public`; all catalog/map/search RPCs require an active exact-cohort grant for that audience and exclude them from anonymous projection, search indexes/sitemaps, caches, and public provider payloads. Creating an invitation stores only the parent token hash and verified-email HMAC. Fragment exchange matches both and, for a new user, atomically burns the parent and issues/returns a `purpose=readiness` Package 2 child registration URL with the parent's seven-day-or-shorter expiry; an existing user signs in and burns the parent without a child. The parent never replays. Lost unused child recovery requires recruiter repeat verification, child/pending-record revocation, and a new parent invitation. Acceptance requires authenticated matching verified email and consent, marks the pending record accepted, then creates a 30-day cohort grant. Invitation revoke/expiry before exchange or verified recovery after exchange revokes every unused child and denies the next request; Package 2 owns cross-system retry/orphan cleanup. Test raw-secret handoff, atomic burn, old-parent replay denial, lost-child new-parent recovery, new/existing user, wrong email, receipt mismatch/revoke, provider/application crash boundaries, and no cohort grant before completed verification/consent. No scraped/bulk data, unlicensed description/media/review, or partnership implication. Required 12/70% and 100% freshness rules apply. Prepare/approve the canonical responsive Topeka Browse route `/stores?area=topeka-ks` with concise product information, print-ready flyer, ordinary public QR/plain URL with no privilege token, and plain store-claim instructions linking to the Package 6 flow. Package 10A tests destination, print contrast/size, accessibility, copy accuracy, and separation from Partner/readiness QR; artifacts remain private/not distributed until 10B.

**CAT-01 cold-start rehearsal (Package 10A Step 0):** before any remaining Package 10A baseline/cohort work, two named reviewers independently prepare/reconcile/verify three non-partner Topeka listings using approved public facts. Record source count, human minutes, disagreements, corrections, and reverification effort. Product Owner approves a catalog-work budget and forecast for 12/70% plus 180-day reverification. The signed CAT-01 receipt unlocks the rest of Package 10A. Stop if the second verifier is unavailable, provenance/license is incomplete, forecast exceeds the budget, 100% freshness cannot be sustained, or the baseline cannot be reproduced.

**Private promotion preparation:** add `promotion_channels`, `promotion_artifacts`, channel-specific `store_promotion_consents`, `campaign_source_codes`, and `campaign_daily_aggregates`. Preview canonical `/stores?area=topeka-ks`, Store Share, sitemap/robots transition, fact-only LocalBusiness data, rights-cleared social image, flyer/plain-URL/ordinary QR, consent withdrawal/removal, and failure destinations. Store Details provides one labeled `Share this store` action using the canonical public store URL and optional allowlisted opaque `src`; use native Web Share when available and a `Copy link` fallback otherwise. Cancel writes no metric; count at most one aggregate Share action per explicit successful share/copy invocation, announce success/failure accessibly, and retain no recipient/app identity. Codes are random allowlisted and carry no identity/authority/location. Measurement has daily aggregate opens/Details/Share only, 180-day retention, no cookie/device/fingerprint/IP identity/account linkage, and no owner analytics. Artifacts remain private/noindex/undistributed; withdrawal marks Do Not Distribute and blocks reprint/post.

**Routes/states/commands:** `/readiness/join#token=...`, `/readiness/status`, and `/admin/readiness`. Invitation is `pending`, `registration_pending`, `accepted`, `revoked`, or `expired`; grant is `active`, `revoked`, or `expired`; run is `not_started`, `in_progress`, `completed`, or `blocked`; receipt is `draft`, `frozen`, `signed`, or `rejected`. Commands are `create_readiness_invitation`; `accept_readiness_invitation`; `revoke_readiness_grant`; `begin_readiness_run`; `record_readiness_step`; `submit_readiness_return_intent`; `exclude_readiness_subject`; `calculate_readiness_gate`; `freeze_readiness_receipt`; `sign_readiness_receipt`. Invitation/accept/revoke commands are idempotent and exact-email bound. `record_readiness_step` accepts only a server-issued one-use challenge from a successful Browse/Details response or validates the caller's actual Plan/Go/private-memory resource and state; clients cannot write arbitrary completion. Gate calculation reads frozen source facts/events, never editable client totals.

**Cohort:** invite at most 20 verified-email Topeka adults directly. Before any first attempt, the selected first eight must also satisfy the older-adult composition: all age 55+, at least three 70+, and at least two self-reporting a low-vision, motor, or assistive-technology adaptation. Store only `55â€“69`/`70+` and adaptation yes/no for scheduling; delete these attributes 30 days after gate signature/rejection and retain aggregate counts. Require age 18+, current test-privacy consent, one person/account attestation plus secret-HMAC verified-email deduplication, and a 30-day grant. Exclude owner/household/AI/Synthetic/test operators as defined by PRD. Stop new enrollment when eight begin; the same eight may retest after fixes, with every original/retest result preserved, but replacements cannot erase failures. No precise location is collected for evidence. Reviews stay disabled.

**Ownership/lifecycle:** invitees read only their own consent/grant/run status and export only that personal status; they never read another subject, internal exclusion reason, or gate totals before publication. Operations creates invitations and may record a reason-coded exclusion; Trusted service validates steps/calculates; Product Owner alone signs. Raw tokens delete within 24 hours after terminal state; exact email/user linkage and detailed run events delete 30 days after 10B release or gate rejection; pseudonymous event counts, exclusions, artifact hashes, and signed receipt retain three years, are excluded from shopper export, and contain no precise location/content. Application/telemetry logs contain no token, email, accessibility attribute, route payload, or per-user journey.

**Evidence:** three unique three-store itineraries per named day for one non-holiday Tuesday, Friday, and Saturdayâ€”nine totalâ€”within 30 days after baseline recheck. Each uses a unique store set, starts at first-store opening, allows 45-minute dwell plus 10-minute transitions, uses the accepted-provider matrix, finishes every visit by verified closing, and preserves dates/hours/matrix/order/calculation. Also preserve shopper attempt/completion/return intent, browser/device and older-adult results, and legal/insurance/support/security/recovery/incident artifacts. Metric events use pseudonymous subject IDs, dated window, inclusion/exclusion reason, and content-free counts. Gate calculator is deterministic from frozen receipt inputs and cannot be edited after signature.

**Failure/rollback/stop:** revoke cohort and hide real listings on expiry, withdrawal, defect, or failed gate; preserve required receipts only. No Package 10A action enables anonymous access, search indexing, public reviews, or public/production promotion.

## Package 10B â€” Regional promotion and Topeka release

**Prerequisite/outcome:** complete signed Package 10A receipt; accepted H/E/R/M/L/S provider gates as applicable; HC-02; SEC-01; B-01 final brand/domain; 99.5% availability/capacity proof; production DB/Auth/Storage restore proving 15-minute RPO/four-hour RTO. Supabase Free does not satisfy the current recovery proof, so release remains blocked until the Product Owner approves paid recovery or a validated `$0` equivalent. Promote the same tested artifact/schema/config, exact frozen catalog set, claims, public reviews, and approved product promotion, and record release.

**Commands/state:** `promote_release(candidate_receipt_id, artifact_digest, migration_set_digest)` locks the Package 2 registration quarantine latch first, requires `latch.state=open`, holds that lock through the entire release transaction, and requires Product Owner plus Engineering/Security/Operations evidence roles; `promote_regional_catalog(candidate_receipt_id, frozen_store_ids)` locks the signed receipt and atomically changes only that exact set from `regional_readiness` to `public` after rechecking two-person provenance, required-field freshness, no pilot/private fields, rights/consent, no duplicate/closure/hold, and exact area. It builds the public projection without copying cohort, claim evidence, private owner data, readiness events, or pilot grants. Any set mismatch or failed store rolls back the whole transaction. Deployment-only `set_server_capability` changes public catalog/claims/reviews/promotion only inside promotion and invokes Package 2 `set_account_registration_mode(public, candidate_receipt_id, expected_version)` in the same release transaction; no second registration command exists. Either `draining` or `blocked`, including a concurrent attempted transition, rolls back catalog publication, registration, claims, reviews, and promotion together. `rollback_release` disables new capabilities first, sets registration `closed`, and withdraws the exact promoted projection while retaining the restricted source/receipt for safe repair. `release_receipts` record the frozen store set plus actor approvals, artifact/migration/config hashes, DB/Auth/Storage recovery, capacity/cost thresholds, monitoring, promotion consents/artifacts, smoke tests, and time.

**Public catalog abuse boundary:** before promotion, revoke browser/`anon` execution on `catalog_list` and `catalog_details`. All anonymous public reads use the single typed Supabase Edge Function gateway and numeric limits defined in `SECURITY_AND_TRUST.md`; no public route may fall back to direct PostgREST. H-01 proves trusted platform client-address metadata, atomic counters, list/detail/burst/concurrency/global-cost boundaries, direct-RPC bypass denial, proxy/IP-version behavior, accessible 429/retry copy, and `$0` no-overage degradation. Failure leaves `public_catalog_enabled=false` while local/Synthetic Package 1 behavior remains unchanged.

**Execution:** recovery point; migration dry-run; config/secret/digest/SBOM validation; canary private smoke; production migration; capability enable; anonymous/catalog/review/auth/claim/Store/Admin smoke; publish canonical Topeka Browse/current Store pages/sitemap; distribute only approved opt-in flyers/ordinary QR, voluntary partner/founder posts, permissioned community/tourism/calendar/earned-media channels; synthetic deny; monitoring/status; signed receipt. No paid media, scraped outreach, unsolicited bulk email, sponsored ranking, affiliate/ad network, or private-data targeting. Public release does not authorize another community; Package 11 RG-01 controls that separately.

**Rollback/stop:** predefined alerts for authorization/privacy/data loss, migration errors, availability/recovery failure, elevated error/latency, 75% quota forecast, consent withdrawal, broken/substituted QR, unauthorized partnership copy, stale listing, spam, support overload, or incorrect publication pause affected promotion/capability. At 90% stop new promotion and optional provider/media/email functions before core safety. Reapply deletion/revocation receipts before reopening. Any failed smoke/incomplete receipt leaves public capability off. Review a channel after four weeks and at least 50 attributed opens; Product Owner records continue/change/stop without an invented conversion threshold.

## Package 11 â€” RG-01 Topeka success evidence

**Prerequisite/outcome:** begin after a signed Package 10B receipt, required production smoke/monitoring checks currently pass, and no release stop condition is active; no added elapsed-time minimum applies. Calculate/sign RG-01 from live Topeka evidence. Passage permits only Product Owner consideration of a separately gated first-community plan; it never selects a community, imports listings, contacts owners, promotes, or activates expansion.

**Window/pass:** choose and freeze one rolling window no longer than 180 days; no minimum elapsed duration. Pass only when: at least 25 distinct eligible shoppers complete a first qualifying trip; at least 10 of those complete a second qualifying trip on a later calendar date; 100% active discoverable listings are current; at least three active exact-store consented flyer locations; zero open Blocking/privacy/security/data-loss defects; and `10 Ã— new_support_cases <= 10 Ã— active_store_count + qualifying_completed_trip_count`. Claim conversion is approved eligible claims divided by eligible submitted attempts with rejected/abusive attempts separate; it is reported, not pass/fail.

**Eligibility/source:** use authoritative server trip completion, listing freshness, exact-store flyer-consent, defect, support, and claim recordsâ€”not client counts. Eligible shopper is one consenting 18+ human/nonprivileged account with Topeka area, excluding Scott, Independent Internal Tester, AI/Synthetic/test operators, duplicate humans/accounts, and Representative own-store activity. A qualifying trip has at least two active Topeka stores and `Done Here` for at least two on one calendar date. Second trip follows server completion order and a later calendar date.

**Schema/commands:** restricted consent/subject ledger using receipt-local pseudonyms and purpose-specific HMAC/key version; frozen source manifest/cutoffs; metric/exclusion rows; claim report; calculation runs; signed/superseding receipts. Commands: own consent/withdraw; exact-store Representative flyer consent/withdraw; operations prepare/recalculate/freeze; trusted service derive; Product Owner sign/reject. Shopper never reads totals. Administrator role alone cannot sign. Client-supplied counts, store lists, exclusions, formulas, or pass state are rejected.

**Privacy/lifecycle:** never copy trip contents, notes, reviews, coordinates, support content, claim evidence, or defect text into RG-01. Exact linkage exists only while calculating and deletes within 30 days after signed/rejected disposition; replace subjects with receipt-local random labels and destroy the dedup key. Retain aggregate counts, source hashes, exclusion codes, formulas, claim report, and signed receipt three years. RG-01 never extends source-record lifetime. Flyer withdrawal before freeze removes eligibility; after signature it requires a superseding run/receipt.

**Failure/verification/rollback:** any missing denominator, target failure, stale source, withdrawn consent, unresolved defect, unverifiable exclusion, signature mismatch, or frozen-source mutation leaves expansion blocked. Source change after freeze creates a superseding run; no result auto-opens geography. Test 180-day boundaries, duplicate/excluded subjects, trip ordering/later date, own-store exclusion, exact integer support formula boundaries, freshness/flyer withdrawal/defect snapshot, claim reporting, consent withdrawal, linkage purge/key destruction, deterministic replay, frozen-source mutation, Product Owner-only signature, no content/location retention, and no client-authored total.

## Package 12 â€” One-community activation run

**Prerequisite/outcome:** this is one repeatable per-area run, ordinal 1â€“3. Run 1 begins only after a signed passing Package 11 RG-01 receipt and a separate Product Owner decision naming one Eligible Small Community. Runs 2â€“3 each require a new Product Owner community selection plus a signed passing Community Expansion Gate receipt for the previously activated community. One run prepares and activates exactly one named area; it never chooses another area or authorizes Kansas City/larger-metro work.

**Private preparation/readiness:** create an exact area slug and frozen candidate-store set; privately recruit the approved willing anchor owner through the Package 6 invitation/consent/authority flow; verify at least two active listings with two-person provenance; prove exact catalog/consent, two separate-phone internal trips, monitoring, recovery, security, support path/capacity, quota, rollback, and zero Blocking/privacy/security/data-loss defect. Product Owner signs this preactivation receipt. No public listing, owner implication, or promotion occurs before that signature. Non-partner listings remain fact-only.

**Sequencing state:** restricted singleton `community_expansion_root(id=1, last_activation_ordinal 0..3, last_attempt_sequence bigint, active_run_id nullable, version)` and `community_activation_runs(run_id, attempt_sequence UNIQUE, target_ordinal 1..3, activation_ordinal nullable UNIQUE, area_id, selection_receipt_id UNIQUE, rg01_receipt_id nullable, prior_gate_receipt_id nullable, state prepared|readiness_signed|live|withdrawn|cancelled, version, readiness_receipt_id, cancellation_receipt_id nullable, activation_receipt_id nullable, rollback_receipt_id nullable, reactivation_receipt_id nullable, artifact_binding_digest, activated_at nullable, cancelled_at/reason nullable)`. A partial unique index allows at most one non-cancelled run per area; a cancelled preactivation attempt does not reserve the area. Run targeting ordinal 1 requires the signed RG-01 receipt and no prior gate; targets 2â€“3 require the exact immediately prior activated area's signed passing Community Expansion Gate receipt and no RG-01 substitution. Selection receipts are Product Owner-signed, bind one area and expected prior receipt, and never select more than one area.

**Commands/release:** every mutation locks `community_expansion_root` then run then catalog projection, uses expected root/run versions plus a unique idempotency key, and stores the signed action receipt before changing state. Same key/same input or a lost-response repeat against the resulting state returns prior success; same key/different input or conflicting receipt denies. `prepare_community_activation(area_id, selection_receipt_id, expected_root_version, idempotency_key)` derives `target_ordinal=last_activation_ordinal+1`, increments `last_attempt_sequence`, validates the RG-01/prior-gate chain, creates exactly one run, and sets `active_run_id` atomically. Duplicate active/live area, duplicate selection, concurrent active run, wrong prior receipt, or target ordinal 4 denies. `freeze_community_catalog` creates nonpublic staging only. `sign_community_readiness(run_id, readiness_receipt_id, expected_run_version, idempotency_key)` binds its frozen digest. `cancel_community_preparation(run_id, cancellation_receipt_id, reason, expected_root_version, expected_run_version, idempotency_key)` permits only `prepared|readiness_signed`, marks `cancelled`, clears `active_run_id`, and does not advance `last_activation_ordinal`; a new signed selection receipt may retry the same area or choose another at the same target ordinal, while attempts are never reused.

`activate_community(run_id, activation_receipt_id, expected_root_version, expected_run_version, idempotency_key)` is the sole first-promotion command and one transaction validates signed frozen artifacts, changes the exact catalog audience to public, binds digests, sets `state=live` and `activation_ordinal=target_ordinal`, advances `last_activation_ordinal`, and clears `active_run_id`; any failure exposes nothing and changes none of those fields. `rollback_community(run_id, rollback_receipt_id, expected_root_version, expected_run_version, idempotency_key)` applies only after `live`, atomically withdraws the projection and marks `withdrawn` without changing the activation ordinal. A withdrawn area blocks the next preparation but is repairable: `reactivate_community(run_id, reactivation_receipt_id, expected_root_version, expected_run_version, idempotency_key)` requires a new Product Owner-signed frozen repair/readiness/recovery/capacity receipt for the same area/store set, atomically restores its public projection and `live` state, and neither advances nor clears/reuses any ordinal. Product Owner signatures bind area, store IDs, artifact/schema/config digests, recovery/capacity, channel consents, canonical route, and allowlisted aggregate `src` codes. Tests cover rootâ†’runâ†’projection lock order, cancel-vs-activate, double/lost-response activate, activate-vs-rollback, rollback-vs-reactivate, first/second/third activation, attempt sequencing, cancel before/after readiness, owner withdrawal before activation, retry same/different area, wrong replay/receipt/gate, crash before/during atomic visibility, rollbackâ†’repairâ†’same-ordinal reactivationâ†’gate, and fourth-activation denial.

**Postactivation Community Expansion Gate/stop:** after activation, collect the current area's anchor-owner independent direct edit, reviewed controlled change, and support request; separate-account/phone multi-stop trips by Scott and the Independent Internal Tester; five voluntary shopper trip-use confirmations without precise-location tracking; monitoring/support/data-accuracy results; and zero open Blocking/privacy/security/data-loss defect. `decide_community_expansion_gate(run_id, gate_receipt_id, decision Pass|Reject, expected_run_version, idempotency_key)` requires the exact `PrimaryInternalTester` evidence responsibility, MFA/recent-auth, and frozen-digest capability; ProductOwner-only and Administrator-only callers deny. `Pass` requires every predicate; `Reject` binds failed-check reasons. No minimum duration applies. Failure pauses/rolls back affected public projection/promotion and always blocks the next area. No later run begins without that exact prior area's passing PrimaryInternalTester receipt. Stop after ordinal 3 for a separately approved larger-metro review. Tests cover exact responsibility, failed-pass denial, authenticated rejection, replay, and wrong prior signer/receipt.


## Package 13 — Photo-tier memberships, moderation, and staged-off billing

**Outcome and stage:** implement store photo-tier subscriptions (free cover+5 / Featured 15 / Unlimited), tier enforcement at upload intake, the Administrator moderation queue for store-submitted photos, Store Portal rejection visibility with resubmit, and the complete Stripe billing integration — all while `photo_tiers_enabled=false` in every environment. The capability flips only through a receipt-bound deployment command after BOTH the post-RG-01 monetization Product Decision AND this package's own activation gates pass. Free-tier behavior (cover plus five approved images, indefinite, no removal for nonpayment) is active for every existing pilot store regardless of the flag. Dollar price points are recorded only in the signed activation receipt; they are deliberately absent from code and config until then.

**Controlling decisions:** `PRODUCT_DECISIONS.md` sections "Photo-tier memberships" (onboarding via invitation→consent→draft→tier selection→Stripe hosted Checkout redirect→Administrator approval before publish; grandfathering; instant prorated upgrades, cycle-end downgrades, last-scheduled-wins, 30-day hidden-photo grace; annual billing deferred; Stripe Tax at activation; cancel-anytime portal + 48-hour full-refund window), "Photo moderation criteria" (accepted types exterior/interior/signage/inventory; rejection list screenshots/watermarks/AI-generated/non-consented people/third-party logos/blurry/off-topic/promo banners; single-Administrator review; two-business-day internal target; reasons visible with resubmit; listings go live without photos), `docs/specs/store-membership-spec.md`, and intake-side enforcement per `docs/research/gallery-photo-data-access.md`. The read path stays uncapped: `catalog_details` continues returning every published `store_media` row unchanged.

**Schema and ownership:** server-owned FORCE-RLS tables only. `partner_private.store_photo_tier_state(store_id unique, tier free|featured|unlimited, source default|subscription, version)` resolves each store's current cap (free=cover+5 grandfathered default); `partner_private.store_subscriptions(store_id unique, stripe_customer_id, stripe_subscription_id nullable, state none|active|past_due|grace|canceled, current_period_end, downgrade_to, hide_photos_after, version)` mirrors Stripe exactly as received from verified webhooks; `media_private.media_uploads` gains nothing new — intake validation reads tier state; rejection reasons reuse the mandatory `approval_reason`; `app_public.stores` remains untouched. No card, token, or bank field ever persists client-side; webhook payloads are not logged.

**Commands:** `resolve_store_photo_cap(store_id)` internal helper consumed by the M-01 intake path so an over-cap upload returns `not_allowed` with plain-language upgrade copy fields (never a raw count leak beyond the shopper-visible cap itself). Moderation reuses the shipped pipeline commands (`media-provider-command` states) with Administrator-role gating already enforced by `approved_by/approved_at/approval_reason` constraints; Portal reads own-store submission state including verbatim rejection reasons; resubmit always creates a NEW `media_uploads` row through standard intake (cap-enforced), never edits a rejected row. Billing commands, all inert under flag-off (`stage_disabled`): `create_checkout_session(store_id)` (hosted Checkout redirect, no card fields on-domain); `stripe_webhook` Edge Function verifying signatures, replay protection, and atomically applying subscription events to the mirror plus audit/outbox rows; `create_portal_session(store_id)`; automated job applying 14-day failed-payment grace expiry → downgrade to free with 30-day hidden-photo grace before deletion per lifecycle rules; refund-window handling is procedural (portal cancellation anytime + 48-hour full-refund request) recorded on the mirror, not custom refund code.

**Flag-off inertness proof (blocking acceptance):** routes `/store-portal/billing`, admin tier screens, checkout/portal session RPCs, and the webhook endpoint return `stage_disabled` or are unreachable end to end; no outbound Stripe call is possible from any environment configuration; RLS/RPC/Edge/route-loader checks each assert the capability like public reviews. Publication still never results from payment alone: Administrator approval remains mandatory for listing publish, and photo publication follows the unchanged M-01 pipeline constraints.

**Jobs/tests/rollback:** grace-expiry/downgrade job follows shared job rules (singleton lock, SKIP LOCKED, idempotent by key). Tests red-first cover: cap boundaries per tier incl. grandfathered free (at-cap rejects next gallery upload while cover logic holds; resubmit counts against cap); over-cap copy fields; moderation authorization matrix (single Administrator role, reason required, synthetic-only visibility, audit chain entries, shopper-private denial); portal reason visibility + resubmit creating new pending row; webhook signature/replay/out-of-order/duplicate handling; grace countdown → automatic downgrade → hiding → deletion; upgrade proration semantics mirrored from Stripe; refund-window recordkeeping; flag-off inertness matrix; and no read-path change (catalog_details payload byte-comparison pre/post). Rollback disables the capability first, then reverts compatible code; mirror-table rollback preserves consent receipts and audit chains. M-01 gate blocks any REAL image processing; synthetic-only uploads remain the test fixture until it passes.
## Independent-build acceptance

Package 1 plus these contracts cover every Regional Public MVP package, postlaunch RG-01, and one-community activation. A builder may select an explicitly gated provider only through its ADR; they may not invent product policy, weaken a denial, enable an absent stage, add post-MVP scope, or skip evidence. If a package-specific implementation detail is not mechanically derivable from this file and the controlling source hierarchy, stop that package and record the exact gap on Issue #1 before code.
