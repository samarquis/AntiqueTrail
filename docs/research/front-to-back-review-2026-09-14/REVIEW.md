# Front-to-back review — 2026-09-14

Date: 2026-09-14. Baseline: `f58439fd` on `main`. Class: operator review of the repository and its evidence, not production, provider, or human/store-showcase evidence.

## Purpose and method

The store-first round asked whether the repository, plan corpus, backend, and hosting are consistent enough to support the bounded regional public test. This review walks front (live web build, CSP, headers) to back (Supabase migrations, session/auth, RPC gates) and through the plan corpus, recording what is observed as evidence and labelling what remains an owner decision.

## Baseline and verification run

- `npm test`: 929 tests passing across 150 files.
- `npm run build`, `npm run typecheck`, `npm run format -- --check`, and the audit run: green.
- `git diff --check`: clean. Tree clean at the review end aside from the corpus repair commits this receipt covers.
- The live public test at `https://antique-trail.vercel.app/` serves `index-CjuQCW9p.js`; a fresh build produces `index-d_DI2u2T.js`. The digests differ only by environment-visible catalog/improvement render flags; the deployed chunk is the catalogue-only build for this test. The per-file digest breakdown is recorded in the private operator spec, not in this dated public document.
- CSP: the global security headers added in PR #383 include the theme-script hash `EgoY60HzbKJvd8C9vXjL5slrJXMqguWBuyKQCVBzuiY=`, which is the verified hash of the current theme script generated by the build toolchain.

## Findings

1. **Implementation is ahead of the plan corpus.** The surviving corpus describes stages whose code already ships (145+ migrations, authentication/RPC gates, store directory/showcase, staging gates). This is a finding, not a defect: the implementation satisfies the documented contracts deeper than the corpus narrates.
2. **The plan corpus is the residual authority.** ADR0009 and the `SECURITY_AND_TRUST.md` and `PACKAGE_CONTRACTS.md` stages are the operative documents; the deleted governance machinery is archived at the historical baseline commit and is not recreated as live authority.
3. **Backend posture.** 144 forward-only migrations with no breaking edits; auth/sessions, role-gated RPCs, and the privileged-operation limiter follow the separated

 principal/authorization model. No backend code changed in this review.
4. **Live public test serves the correct scoped build.** The catalogue-only build is live; the bounded-publication contract and region declaration in README govern what it exposes.
5. **Deployment readiness gates.** H-01, E-01, M-01, and SEC-01 are still NO-GO in the release-gate ledger and remain prerequisite to activation. The substitute-hosting (ADR0010) acceptance receipt is not yet visible in the repository; its publish/review evidence is still pending.
6. **Local CI ergonomics.** Branch protection requires both the `web` and `database` checks; the local `npm run check` fails only because `eslint.config.js` line 8 does not ignore `.worktrees/` directories, so a worktree run trips the lint sweep. Suggested one-line fix: add `.worktrees` to the ignored patterns.
7. **Governance cleanup (PR #383).** The slimming was intentional and owner-confirmed: shortened PRD, slimmed specialist documents, deleted governance machinery, and global security headers. It ran without an append-only receipt and left surviving live documents pointing at deleted files and removed headings. This review repaired those references and recorded the receipt. See `PLAN_CHANGELOG.md` entry `2026-09-14 — Governance corpus slimming, security headers, and cross-reference repair`.

## Repair performed

- Restored minimal PRD anchors (purpose/promise; connected shopper experience; store and administrator experience; next milestone; stage dependencies; deferred implementation boundary; public test publication; provider prerequisites; assessment boundary; human usability acceptance) so surviving links resolve.
- Restored `SECURITY_AND_TRUST.md` (`#public-test-boundary`, `#store-first-stage-applicability`), `PACKAGE_CONTRACTS.md` (`#public-test-execution-contract`, `#store-first-pilot-activation-contract`), and `README.md#source-precedence`.
- Repointed surviving live and hybrid documents (PLANNING_INDEX, PLAN_ACCEPTANCE, CODEX_START_PROMPT, ADR0009, product-capabilities, design README and ICON_PLACEMENT_SPEC, owner-onboarding, product-reset proposal, issue-323 evidence) from deleted files and removed headings to surviving owners (PLAN_CHANGELOG, PRODUCT_DECISIONS, and current specialist sections).
- Link sweep after repair: every surviving local-file anchor resolves to a present heading (0 dead anchors). The only residual is a stored PR-body artifact (`docs/evidence/issue-125/pr-body.md`) whose two links are root-relative for the GitHub PR UI; both targets exist and the file is frozen historical evidence.

## Residual frozen references

Dated artifacts written before the slimming (PRODUCT_DECISIONS history entries, the product-reset-2026-09-06 reconciliation proposal, IMPLEMENTATION_PLAN, and issue-125/310/316 evidence) still reference headings the slimming removed. Those files are frozen records of their date and are not rewritten; a reader treats their links as historical pointers, not live navigation.

## Next actions and owner decisions

- Decide the release gates: H-01/E-01/M-01/SEC-01 receipts remain NO-GO in the ledger and must be satisfied before activation.
- Decide whether the substitute-hosting (ADR0010) acceptance evidence is to be recorded in the repository.
- Optional: apply the `.worktrees` lint ignore so worktree `npm run check` passes.
- No code, asset, migration, provider, or activation action is authorized by this review.