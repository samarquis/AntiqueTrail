# Issue 171 verification

Contract: `PACKAGE_CONTRACTS.md`, Package 6, Add-store commands/lifecycle and Verification/rollback; `PRODUCT_DECISIONS.md`, Add-store stage and evidence. Public activation remains issue 56.

- Ordinary account: real private-owner SQL execution covers search-before-add, shared root reservation, draft, submit and own-only status; the normal authenticated RPC stays stage-disabled and exposes no synthetic selector. React uses the existing material-terms gate and an exact-version RPC client.
- Duplicates: normalized name/address/phone/domain matching runs at search, submit and approval. Confirmation converts under the shared root and carries only content-free references requiring reverification. SQL and browser checks cover confirmation, exact retry, no extra store and root transfer.
- Approval: one transaction writes canonical store, categories/hours, four provenance groups, partnership, exact Representative grants, Free tier and approval/audit receipts. An injected audit failure proves rollback of the store, grant and receipt. Fresh verification, independent evidence objects/events, active email/MFA/consent and current signed regional receipt are server checks; the normal capability remains off.
- Boundaries: 40 focused pgTAP assertions pass, including stale writes, old authority evidence, repeated verification events, cross-applicant status, altered approval replay, second-store denial, export scope and deletion retention. The full migration applies on a fresh local baseline copy. The baseline restore reported the known absent `graphql_public.graphql` grant; hosted clean-reset pgTAP is the independent clean-reset evidence.
- Concurrency: `python scripts/test_store_application_concurrency.py supabase_db_issue170-20260905 issue171_concurrency4` passes on a disposable local database: simultaneous identical approval and conversion retries return one outcome; competing claim/add creates exactly one active intake.
- Mutation: removing authority invalidation makes the focused tests fail for retained verification and approval using old evidence. The mutation was transaction-local and rolled back.
- UI: 15 targeted Playwright checks pass across desktop, tablet and 320px, covering search/save/submit, evidence-reference clearing, duplicate conversion, failed-save preservation, keyboard, dark/forced-colors and explicit administrator approval. Inspected the mobile screenshot. Fixtures are synthetic UI evidence, not server authorization evidence.
- Local checks: full check passed 99 files / 711 tests plus 82 release checks; subsequent affected App/client/HMAC checks passed 31 tests. Typecheck, lint (existing warnings only), formatting, final production build, security contract and production-artifact assertion pass.
- Lifecycle: scheduled account worker invokes retention; profile deletion removes personal draft linkage; scoped exports include own draft/status/outcome and exclude sibling applications. No real mail, media, payment or deployment was exercised.

Independent exact-head review and required hosted `web`, `database`, and `plan-governance` results are recorded on the PR; local evidence does not substitute for those gates.
