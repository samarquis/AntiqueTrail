# Issue 173 verification

Contract: `PRD.md` - Launch promotion and prospective-owner acquisition; `PRODUCT_DECISIONS.md` - Product promotion is not monetization; `DESIGN_SYSTEM.md` - QR destination and Store Portal promotion route clauses. Base: ebdf0e61371d68fa885a56724da1e00eddaef062.

- Artifacts: `node scripts/promotion-artifacts.mjs` generates the two private HTML print fixtures. Three artifact tests decode their actual PNG images, verify exact shopper/owner routes and plain-URL fallbacks, and reject public tokens, attribution, origin/canonicalization tricks and class repurposing. Black/white output has a four-module quiet zone, two-inch image and 16-point print body. Desktop print output was visually inspected. The reserved `.example` origin and Do Not Distribute label are deliberate synthetic evidence; the signed real name/domain and distribution remain #56.
- Invitations: no invitation is minted, stored or printed by the promotion generator. The distinct validator accepts only the existing Partner/readiness fragment routes, without source attribution. Actual approval, expiry and one-time redemption remain enforced by their existing issuers and exchange paths.
- Channels: four separate exact-store permissions, plus generic owner-card permission requiring an active recently authenticated MFA Administrator, carry no prospective recipient identity. Store consent and reads bind the current grant, not merely the actor. Every distribution/reprint/post command checks consent, exact grant, release authority and the staged-off capability; exact-store use also checks the frozen-store rights receipt. A social consent permits one post authorization. Withdrawal requests removal and remains available during release rollback. Content-free events and the existing privileged audit are committed with the mutation; failed audit rolls back consent.
- Privacy: optional source measurement stays off, its allowlist starts empty, and current print fixtures reject all `src`. The server accepts only allowlisted opaque codes and open/details/share kinds, derives UTC day itself and stores only code/day/event/count. It accepts no event timestamp, actor, store, account, device, IP or location field. Daily rows expire at 180 days; content-free channel events expire at three years. Existing signed gate-receipt ownership remains separate; this implementation creates no signed campaign totals. Own channel permissions are included in the scoped account export.
- SQL: 47 pgTAP assertions pass against an isolated migrated database. The migration also applies using the normal `supabase_admin` role, after repairing the required owner-role grant for the existing release-lock helper. Two real concurrent-connection races pass using `python scripts/test_promotion_concurrency.py CONTAINER issue173_DATABASE`: queued reprint loses to withdrawal; a measurement pause waits for an earlier blocked count and later counts deny.
- UI: all 12 focused Playwright cases pass across desktop, tablet and 320px (nine print/anonymous cases, followed by three successful portal rechecks). The first parallel portal run timed out during axe while local Docker recovery/full unit startup competed; the unchanged cases pass with one worker. Consent is channel-specific, withdrawal displays removal status, anonymous access denies, and axe/reflow/print-size checks pass. Three focused client/component tests and all 723 unit tests (102 files) pass. Security-contract and the isolated database security advisor pass. The independent review found an extra EOF blank line in the SQL fixture; it was removed and the full base-to-candidate diff check was rerun.

Independent final-SHA review and required hosted web/database/plan-governance results must pass on the PR before landing. No deployment, real consent, printing, posting, distribution or activation was performed.
