# Domain Docs

This is a single-context repository.

Before exploring implementation work, read `CONTEXT.md` and the relevant decisions in `docs/adr/`. Use the glossary vocabulary from `CONTEXT.md` in issue titles, plans, code, and tests. If an output would contradict an ADR, surface the conflict explicitly instead of silently overriding it.

